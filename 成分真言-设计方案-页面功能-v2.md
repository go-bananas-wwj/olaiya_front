# 成分真言 —— 页面功能设计方案 v2.1（2026-08-15）

> 定位：INCIDecoder 打底（成分词典 + 产品成分速览 + Decode 成分表工具），叠加差异化（真实文献核验、浓度推断、透明匹配分、排行榜、embedding 相似检索）。
> 调研结论：INCIDecoder 无排行榜/对比/匹配分/UGC；行业匹配分（SkinSort/美丽修行/Picky）均为黑盒——我们做**逐项理由可见的透明匹配分**。
> 已拍板：首页完全实用化（3D 瓶子不上线，代码留 lab/hero-demo 备用）；「今日核验」卡撤掉（无真实案例不硬凑）；圆桌、图片鉴伪**一期不上**（路由与文件保留，导航与首页零入口，页顶加「实验功能」灰标，二期可复活）。

## 一期页面清单（9 页）

上线：首页、产品库、产品详情、成分库、成分详情、**排行榜（新）**、对比、**解码（新）**、AI 问答
隐藏：圆桌 `/roundtable`、鉴伪 `/detect`（保留路由文件，摘除所有入口；回归检查：全站 grep 无指向它们的链接）
方法论 `/about`：一期降级为页脚静态段落（P1）

## 全局

- 顶栏平铺 6 项：**产品库 / 成分库 / 排行榜 / 对比 / 解码 / AI 问答** + 全局搜索框（回车进 `/search?q=…` 统一结果页，前端并行调 `/api/products?q=` 与 `/api/ingredients?q=` 分组展示，无新后端）
- 设计令牌沿用定稿：珍珠贝母配色；正文 Quicksand+Noto Sans SC、标题/导航/按钮站酷快乐体、数字 Baloo 2；虚线徽章、玻璃卡
- 文案原则：少字、说人话；估计值必带「估计」语义；查不到如实说，不编造

## 逐页功能清单

### 1. 首页 `/` —— 实用门户（无 3D）
- P0：大搜索框（产品/成分通搜）+ 功效胶囊（美白/抗老/保湿/祛痘）+ 数据规模一行（`/api/stats`：N 产品 · M 成分 · K 条真实文献证据）
- P0：三入口卡：查产品 / 解码成分表 / AI 问答
- P1：排行榜精选（成分证据榜 Top5 + 产品核验榜 Top5，复用排行榜页排序逻辑）

### 2. 产品库 `/products` —— 查询体验
一条粘性工具条（搜索框｜功效胶囊｜品牌下拉｜只看有宣称｜排序）+ 卡片栅格 + 加载更多。158 款体量不做面版式多条件筛选器（过度设计）。
- P0 搜索：250ms 防抖（现有）；q 名称/品牌模糊（现有）；**后端小改**：q 并入 nmpa_id 匹配（用户常直接搜备案号）
- P0 功效胶囊（美白/抗老/保湿/祛痘/舒缓/防晒）：**需新后端参数** `GET /api/products?efficacy=美白`，SQL 层 EXISTS(ProductClaim.claim LIKE…)，枚举→关键词映射写死后端（美白→美白/淡斑/提亮，抗老→抗皱/紧致/淡纹，多词 OR；拿不准的宣称不归类，同铁律 7 口径）
- P0 品牌下拉：现有 `/api/brands`
- P0 排序：**需新后端参数** `sort`（claim_count_desc/ingredient_count_desc，默认现状）。**不做**达标率/成本排序：宣称产品与浓度推断产品无交集，排出来是空榜，违反诚实原则（这两个维度只在排行榜页限定子集呈现）
- P0 卡片：名称/品牌/备案号/宣称数/成分数（现有）+ 匹配分角标（已填肤质档案才显示，纯前端 localStorage + 详情缓存）。「够量/存疑」标签不进卡片（需逐产品调 concentration = N+1，且仅国际大牌有值）
- P0 空态：筛选无结果 →「一键清空筛选」；全库查无 →「试试解码成分表」跳转
- P0 保留：竞态守卫（AbortController）、加载更多去重锁

### 3. 产品详情 `/products/:id` —— 全站枢纽
已实现 ✅：判决条、宣称核验卡（三档徽章）、剂量对照条 + 每起效成本、成分速览行内展开、平替区 + 加入对比（URL 带参）
- P0 **功效指纹雷达条**（新）：横向条形图（维=规范功效族，值=剂量因子×证据强度，`/fingerprint` 现有 API 纯前端），置顶宣称核验区上方，一眼看懂「这瓶什么定位」
- P0 **匹配分模块**（机制见下）
- P0 相似区升级：现有 SimilarLevels 三栏 + **第四栏「语义相似」**（`/similar` BGE-M3 向量，徽章标「语义」）；页内注明「相似 ≠ 功效相同」
- P1 Highlights 配方特征标签（无酒精/无香精/含防腐剂，成分表规则推出，注明口径）

### 4. 成分库 `/ingredients`
- P0 搜索 +「只看有证据」；卡片：中文名/INCI/功效族/证据条数
- P1 按功效族浏览（efficacy_canonical；一期前端聚合，后端过滤参数列 P1 新增）

### 5. 成分详情 `/ingredients/:id` —— 词典页
- P0 一句话档案（功效+证据档+起效浓度区间）、别名（USAN 核验通道）、功能标签（CosIng 申报功能，注明非功效实证）、断言+文献链（PMID 跳 PubMed，弱证据 note 如实标注）、安全上限（法定/CIR/SCCS 注明机构性质）、透皮判定徽章、含该成分产品列表（折叠计数条）
- P0 **相似成分条**：`/api/ingredients/{id}/similar` Top-5 芯片链到对应成分页，注明「按证据文本语义相似」
- P0 保留：换路由清空已展开列表

### 6. 排行榜 `/rankings` —— 新页面
- P0 **成分证据榜**：真人实验证据数 + 起效浓度明确度
- P0 **产品核验榜**：剂量达标宣称比例（限有浓度推断的产品子集）；**性价比榜**：每起效成本（同子集）
- P0 顶部固定口径横幅：「本榜按证据强度/性价比排序，不是效果排名」；无数据项不排不猜
- 不加相似检索：相似是关系信号不是排名信号，硬塞稀释榜单口径

### 7. 对比 `/compare`
- P0 招牌大数字卡：每起效成本对比；共有/独有成分 diff（默认只显示差异行）；剂量并排对照条
- P0 URL 带参分享 + 预设对比组
- P0 选择器下方「不知道和谁比？→ 从相似产品里挑」（调 similar-levels L1 填第二槽）
- P1 可搜索 combobox（3 次按键定位，手写不引重型库）

### 8. 解码成分表 `/decode` —— 新页面（INCIDecoder 核心工具）
- P0 粘贴成分表文本 → 逐成分匹配库 → 证据报告（有证据成分列断言与文献；未命中如实列「库中无此成分」）
- 匹配通道：IECIC 中文名 / INCI / USAN 别名（若 `/api/ingredients?q=` 不覆盖别名折叠命中，列为**需新后端参数**项）
- 拍照 OCR：二期

### 9. AI 问答 `/chat`
- P0 流式回答 + 逐句引用 [n] 悬停出证据卡、点击跳 PubMed；示例问题用用户原话；幻觉引用如实红标

## 匹配分机制（透明版，100 分制）

- **用户输入**：肤质（干/油/混合/敏感）+ 诉求（美白/抗老/保湿/祛痘，多选）；存 localStorage 随时可改
- **位置**：产品详情页模块 + 产品库卡片角标（已填档案才显示）
- **加减分逐项可见**（初版规则，实现时校准）：诉求命中且有真人级证据 +20/项；命中项剂量达标 +10/项；敏感肌含酒精/香精/高浓度酸 -15/项；每起效成本低于同诉求中位 +10；无证据支撑宣称 -10/项
- **每条可展开看依据**（挂到哪条断言/哪篇文献）；数据不足产品显示「数据不足，仅供参考」而非给分
- 纯前端计算，依赖现有 `/concentration` 与详情接口，零新后端

## 相似检索诚实口径（每个相似模块页内固定注明）

- 通用：「相似 ≠ 功效相同；向量/集合相似是配方或文本接近度，不构成功效承诺」
- L2 剂量级：剂量为模型估计值（沿用「估计」徽章）
- L3 功效级：相对排序信号，法规/防腐族不计分
- 语义相似（BGE-M3）：索引缺失时显示「相似索引未构建」，不报错不猜
- 空态一律「暂无可比对的产品」，不凑数
- 不做独立「找相似」页面、不做 2D 散点图（158 点无信息量，引入降维新依赖与「降维失真」口径债）

## 需新后端的汇总（最小设计）

1. `/api/products` 加 `efficacy`（功效枚举单选，SQL EXISTS 关键词映射写死后端）——P0
2. `/api/products` 加 `sort`（claim_count_desc/ingredient_count_desc）——P0
3. `/api/products` 的 q 并入 nmpa_id 匹配——P0
4. `/api/ingredients` 的 q 覆盖别名折叠命中（解码页依赖）——P0（解码页）
5. `/api/ingredients` 加 `efficacy_canonical` 过滤——P1

## 明确不做（YAGNI + 叙事保护）

- 用户评分/评论/社区；用户上传产品+人工审核；黑盒分数（任何分数必须可展开复核）
- 首页 3D/运镜；圆桌、图片鉴伪（一期）；独立找相似页；2D 相似散点图；面版式多条件筛选器

## 验收标准

- 一期 9 页上线且互通（产品↔成分↔对比↔排行榜织网）；圆桌/鉴伪路由可达但全站零入口链接
- 排行榜三榜可排、口径横幅在；无数据项不排
- 匹配分逐项理由可展开；数据不足产品显示「数据不足」而非给分
- 解码页粘贴 OLAY 小白瓶成分表出完整证据报告，未命中如实列「库中无此成分」
- 产品库功效胶囊筛选、排序、备案号搜索可用；快速切换筛选无竞态污染
- 数据铁律全量保持：估计语义、诚实空态、弱证据标注、文献可跳
- `cd frontend && npm run build` 通过；`.venv/bin/python -m pytest` 全绿

## 二期候选

圆桌核验（4 角色+裁决卡+预载快照）、图片鉴伪（AI 生图检测+示例一键演示）、解码页拍照 OCR、方法论独立页、成分库功效族后端过滤参数
