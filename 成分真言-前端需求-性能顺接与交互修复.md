# 成分真言 - 前端需求：性能顺接与交互修复（2026-08-09）

后端已完成 N+1 与分页改造（提交 `49ba3fa`），前端当前为过渡实现（Products/Ingredients「加载更多」、IngredientDetail「展开更多」）。
本文档列出需要前端智能体完成的改进，按优先级排序。后端接口契约见文末，**后端不再改动，前端按契约对接**。

技术栈：React + Vite + Tailwind，源码 `frontend/src/`，构建到 `web/`（`cd frontend && npm run build`）。

## P0 交互 bug（审查实测发现，必修）

### 1. 过滤/搜索竞态：过期响应覆盖新结果
- 位置：`frontend/src/pages/Products.jsx:22-37`，`Ingredients.jsx` 同构
- 现象：旧代码用 `alive` 标志在响应落地时拦截；现实现只在 250ms 防抖定时器点火前检查，请求发出后若过滤条件已变，迟到的旧响应会覆盖新列表（offset=0 替换）或把旧条件的下一页**追加**进新结果（offset>0 的 `[...prev, ...page]` 分支）
- 要求：每次请求分配递增序号（或用 AbortController），响应落地前校验是否最新一次请求，过期响应直接丢弃；翻页追加同样受保护

### 2. 成分详情页换路由不重置已展开列表
- 位置：`frontend/src/pages/IngredientDetail.jsx:29,35`
- 现象：`extraProducts` state 不随路由 `id` 变化清空；从成分 A 跳到成分 B 时，A 的「展开更多」产品会混入 B 的列表
- 要求：`useEffect(() => setExtraProducts([]), [id])`，或路由层给组件加 `key={id}`

### 3. 「加载更多/展开更多」快速双击重复追加
- 位置：`IngredientDetail.jsx:37-43`，Products/Ingredients 加载更多同理
- 现象：`disabled={loadingMore}` 依赖重渲染生效，同一拍内两次点击会以相同 offset 并发两个请求、重复追加
- 要求：用 ref 锁或函数式 setState 内去重（按 offset 判重），保证同一 offset 不会并发请求

## P1 可用性改进

### 4. 对比页产品选择器
- 位置：`frontend/src/pages/Compare.jsx:17-19,54`
- 现象：两个原生 `<select>` 各渲染 3749 个 `<option>`，不可搜索
- 要求：换成可搜索选择器（输入过滤 + 键盘可选）。数据仍走 `GET /api/products`（无参，纯 list，约 0.2s）或改分页搜索均可，选简单的
- 约束：不引入重型组件库，手写 combobox 即可，贴现有 Tailwind 风格

### 5. 分页体验完善（可选）
- Products/Ingredients 当前「加载更多」可用；如要做页码/无限滚动，保持接口契约不变（limit/offset）
- 移动端长列表卡顿如仍存在，可考虑简单虚拟化（先实测再决定，不过度设计）

## 新增需求（2026-08-10 CosIng 证据上线后）

### 6. 证据徽章补 `database` 类型中文标签
- 位置：`frontend/src/components/AssertionCard.jsx:1-6`
- 现象：`TYPE_LABEL` 只有 paper/patent/regulation/review，成分详情页新增的 354 条 CosIng 断言（API 输出 `ev.type` 为小写 `database`）会显示裸英文 "database"
- 要求：加一行 `database: { text: '官方数据库', cls: ... }`；顺带核对 regulation/white_paper 等既有类型标签是否齐全（后端 EvidenceType 现有 paper/patent/regulation/white_paper/database 五类）

## 后端接口契约（已实现，勿改）

- `GET /api/products`：无参 → 纯 list（旧契约）；带 `limit>0` 或 `offset>0` → `{total, items}`。支持 `brand`、`q`、`has_claims` 过滤（SQL 下推）
- `GET /api/ingredients`：同上双形态；支持 `q`、`has_evidence`
- `GET /api/brands` → 品牌名 list（约 3ms），供下拉使用，不要再拉全量产品提取品牌
- `GET /api/ingredients/{id}`：新增 `product_limit`（默认 50，0=不限）、`product_offset`、`product_total` 字段
- 分页参数后端会校验 `ge=0`（负 offset 返回 422，正在本次后端任务中补）

## 验收标准

- 快速连续切换品牌/搜索词，列表始终与最终条件一致（乱序响应不污染）
- 成分 A→B 跳转，B 的产品列表无 A 的残留
- 快速双击加载更多，无重复条目
- 对比页可在 3 次按键内定位任意产品
- `cd frontend && npm run build` 通过，`web/` 产物更新

## 新增需求（2026-08-10 CIR/SCCS 数据复审后）

### 7. CIR 浓度字段展示文案修正（误读修正，优先级高）
- 位置：`frontend/src/pages/IngredientDetail.jsx:12-13`（`PRIOR_DEFS`）
- 现象：`cir_conc_low/high` 现显示为「CIR 安全浓度下限/上限」，语义错误——该字段是 CIR 报告中 **PCPC 行业调查的使用浓度区间（全品类合计值）**，不是安全限值，也不是功效起效浓度；「安全浓度」表述会让用户误以为超出上限即不安全
- 要求：
  - 两行合并为一行展示区间：`['cir_conc_range', 'CIR 行业调查使用浓度区间', '%']`，渲染为 `X% ~ Y%`（low/high 都有时）；只有 high 时显示「最高 X%」；只有 low 时显示「自 X% 起」
  - 悬停或行内小字注明含义：「CIR 行业调查报告（PCPC 使用调查）中全行业实际使用浓度范围（全品类合计），非安全限值、非功效起效浓度」
  - `sccs_limit` 标签由「SCCS 限值」改为「SCCS 评估上限（最严场景）」，注明「该意见内多场景结论取最严值，分场景上限见断言详情」
- 约束：后端字段名与接口契约不变，仅改前端展示层

## 新增需求（2026-08-11 竞品借鉴三接口上线后，后端已上线 8008 并验证）

### 8. 证据充分度面板（产品详情页，借鉴 EWG「数据充分度」）
- API：`GET /api/products/{id}/evidence-profile` → `{assertions_total, ingredients_total, ingredients_with_assertions, by_level: [{level, label, count, ratio}]}`
- 形态：分布条 + 分级徽章，放产品详情页证据区顶部；`by_level` 已按证据强度降序、`label` 中文已附，直接渲染
- 要点：**unknown（「证据不足」）如实展示、不弱化不隐藏**——「不知道」是卖点；汇总句示例「46 个成分中 8 个有功效断言，共 28 条：11% 人体 RCT、18% 开放人体试验、32% 证据不足」

### 9. 白盒平替得分卡（产品详情页，借鉴真我得分卡但白盒化）
- API：`GET /api/products/{id}/substitutes?k=5` → `substitutes: [{id, name, brand, score, components: {ingredient: {score, shared, union}, efficacy: {score, dimensions, top_shared_dims} | null, price: {similarity, target_price, candidate_price} | null}, weights_used: {ingredient, efficacy, price}}]`
- 形态：匹配得分卡（score 0-1 可转百分制）；**点开可拆解**：成分重合度（附 shared/union）、功效相似（附共享功效方向）、价格比值（附双方价格）；null 维度显示「无数据，未计入」而非隐藏，`weights_used` 展示实际计权
- 文案：`score` 必须注明「成分/功效/价格三维相对排序信号，非功效承诺」
- 与现有 similar-levels 的关系：得分卡是组合视图，可与三级相似并列展示；成分维数字与 L1 同口径，不得出现两套不一致的 Jaccard

### 10. 成分渗透率（成分详情页，借鉴 INCI Beauty）
- API：`GET /api/ingredients/{id}/penetration` → `{product_count, total_products, penetration, avg_position}`
- 形态：一行统计卡，示例「全库 24.0% 的产品含该成分（899/3749），平均位次 6.9」
- 文案：`avg_position` 附注「各产品成分表长度不同，位次仅供粗略参考」

### 11. 浓度展示保守化（设计调整，优先级高）
- 背景（2026-08-11 拍板）：成分表**位次**和 **1% 线区域**（全量/微量）是备案官方事实，保留并**突出展示**；**百分比区间是估计值**，前端降级，不再单独突出数字估计
- 要求：
  - 成分表行内突出「第 N 位」与「全量成分（1% 线以上）/ 微量成分（1% 线以下）」标识（数据在 `/api/products/{id}` 的 position 字段与现有浓度推断）
  - `/api/products/{id}/concentration` 的 p5/p95 区间不再以大数字卡片呈现，降级为折叠的次级信息，且必须紧邻「模型估计值」语义说明
  - 剂量达标判定（dose verdict）文案统一保守化：用「大概率达到有效量 / 可能不足」级措辞，附「最终浓度以厂商配方为准」
- 约束：后端接口契约不变，仅改展示层与文案

### 验收标准（8-11）
- 产品详情页可见证据充分度分布（含「证据不足」项）与平替得分卡拆解
- 成分详情页可见渗透率统计卡
- 浓度估计不再以大数字形式上屏，位次/1% 线标识清晰可见
- `cd frontend && npm run build` 通过，`web/` 产物更新

## 新增需求（2026-08-11 原料商资料 + 配方典型用量上线后）

### 12. 证据徽章补 `supplier` 类型 + 成分详情展示典型用量
- 位置：`frontend/src/components/AssertionCard.jsx`（TYPE_LABEL）、`frontend/src/pages/IngredientDetail.jsx`（PRIOR_DEFS）
- 后端变化：
  - `EvidenceType` 新增 `supplier`（原料商资料宣称，断言措辞「原料商宣称：XX」，evidence_level 恒 unknown），API 输出 `ev.type` 为小写 `supplier`
  - `GET /api/ingredients/{id}` 的 `priors` 新增 `typical_use_low`/`typical_use_high`（配方实践典型用量 %，52 个成分有值）
- 要求：
  - TYPE_LABEL 加 `supplier: { text: '原料商资料', cls: ... }`，样式建议与 patent 同级（弱证据色系）；断言卡 note 已含「未经同行评议」字样，原样展示
  - 注意：PRIOR_DEFS 现为「单 key → 单值」渲染循环（`[key, label, unit]`），而 typical_use 是两个 key（`typical_use_low`/`typical_use_high`），不能直接加进 PRIOR_DEFS——参考第 7 项 cir_conc 区间的做法，由实现者在 PRIOR_DEFS 循环外单独合成区间行渲染 `X% ~ Y%`（只有 high 显示「最高 X%」，只有 low 显示「自 X% 起」），注明「19 个剂型配方实例聚合的参考用量，非官方限值、非功效起效浓度」
- 约束：后端契约不变，仅展示层

### 验收标准（12）
- supplier 断言显示中文徽章「原料商资料」，无裸英文
- 甘油/烟酰胺等成分详情页可见「配方实践典型用量」行及口径说明

## 新增需求（2026-08-15 工具书断言通道上线后）

### 13. 证据徽章补 `book` 类型
- 位置：`frontend/src/components/AssertionCard.jsx`（TYPE_LABEL）
- 后端变化：`EvidenceType` 新增 `book`（行业参考工具书记载，断言措辞「工具书记载：XX」，evidence_level 恒 unknown，强度 0.3），API 输出 `ev.type` 为小写 `book`
- 要求：
  - TYPE_LABEL 加 `book: { text: '工具书', cls: ... }`，样式建议介于 supplier（弱证据）与普通文献之间；断言卡 note 含书名+条目编号+「行业参考工具书记载，非原始研究证据」字样，原样展示
  - 与 supplier 不同：book 断言**计入**功效指纹与 substitutes 聚合（低强度参与），不需要在相关 UI 做排除提示
- 约束：后端契约不变，仅展示层

### 验收标准（13）
- book 断言显示中文徽章「工具书」，无裸英文
