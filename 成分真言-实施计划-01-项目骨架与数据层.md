# 成分真言 · 实施计划 01：项目骨架与数据层

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭建 git 仓库骨架与 Python 开发环境，完成「证据 / 成分 / 功效断言 / 产品」四层数据模型、真实种子数据加载器，以及一个可启动的最小只读 API（成分列表 + 成分证据链详情），全部测试通过。

**Architecture:** SQLAlchemy 2.x 声明式模型 + SQLite（开发期，DATABASE_URL 可切换）；数据铁律「功效断言必须挂证据」用数据库外键 NOT NULL 强制；种子数据 JSON 提交进仓库，加载器幂等可重复执行；FastAPI 最小只读层验证整条数据链路可用。

**Tech Stack:** Python 3.11 · SQLAlchemy 2.x · Pydantic Settings · FastAPI · pytest · SQLite

## 全局约束（每个任务都隐含遵守）

- 项目根目录即 `/root/workspace/olaiya`，代码直接建在此目录下。
- 统一使用 `/root/workspace/olaiya/.venv/bin/python` 执行 Python/pip/pytest，不使用全局环境。
- pip 已配置清华镜像，安装命令无需额外 `-i` 参数。
- 开发期只用 SQLite；禁止安装/依赖 PostgreSQL。
- TDD：先写失败测试，再写最小实现，再跑通，再提交。
- 每个任务结束必须 git commit；提交信息格式 `type: 中文描述`，type ∈ feat / test / chore。
- 种子数据中的文献引用必须真实可查（标题/期刊/年份/PMID 链接），禁止编造。
- 所有文件 UTF-8 编码；代码注释用中文。
- 测试通过标准：`.venv/bin/python -m pytest` 全绿。

## 后续子计划路线图（本文件只覆盖 01）

| 计划 | 内容 | 对应里程碑 |
|---|---|---|
| 01（本计划） | 项目骨架 + 数据层 + 种子数据 + 最小只读 API | W1-2 |
| 02 | 浓度区间推断引擎（约束传播 + 先验库 + 锚点校准） | W3-4 |
| 03 | 向量层（BGE-M3 + ingredient2vec + Faiss 相似检索 API） | W7-8 |
| 04 | 后端 Agent（LangGraph + 6 工具 + LLM 双通道网关 + 句级引用 RAG） | W5-6 |
| 05 | 可溯源管线与评测（RARR 校验 + RAGAS/FActScore 回归脚本） | W9-10 |
| 06 | 前端（React+Vite+shadcn 对话/核验/对比视图，需先装 Node.js） | W7-10 |
| 07 | 端到端联调、按需求文档第 8 节验收、演示材料 | W11-12 |

## 目标文件结构

```
/root/workspace/olaiya/
├── .gitignore                  # Task 1
├── requirements.txt            # Task 1
├── pytest.ini                  # Task 1
├── backend/
│   ├── app/
│   │   ├── __init__.py         # Task 1（空文件）
│   │   ├── config.py           # Task 2
│   │   ├── db.py               # Task 3
│   │   ├── main.py             # Task 8
│   │   └── models/
│   │       ├── __init__.py     # Task 3
│   │       ├── evidence.py     # Task 4
│   │       ├── ingredient.py   # Task 5
│   │       └── product.py      # Task 6
│   └── tests/
│       ├── __init__.py         # Task 1（空文件）
│       ├── conftest.py         # Task 3
│       ├── test_config.py      # Task 2
│       ├── test_db.py          # Task 3
│       ├── test_evidence.py    # Task 4
│       ├── test_ingredient.py  # Task 5
│       ├── test_product.py     # Task 6
│       ├── test_seed_loader.py # Task 7
│       └── test_api.py         # Task 8
└── data/
    ├── __init__.py             # Task 7（空文件）
    ├── raw/                    # 手工放置的原始文件，git 忽略
    ├── seed/
    │   └── seed_data.json      # Task 7
    └── loaders/
        ├── __init__.py         # Task 7（空文件）
        └── seed_loader.py      # Task 7
```

---

### Task 1: 仓库骨架与开发环境

**Files:**
- Create: `/root/workspace/olaiya/.gitignore`
- Create: `/root/workspace/olaiya/requirements.txt`
- Create: `/root/workspace/olaiya/pytest.ini`
- Create: `/root/workspace/olaiya/backend/app/__init__.py`（空）、`/root/workspace/olaiya/backend/tests/__init__.py`（空）

**Interfaces:**
- Consumes: 无
- Produces: 后续所有任务依赖的 venv（`.venv/`）、pytest 运行方式（仓库根目录 `.venv/bin/python -m pytest`）、git 仓库

- [ ] **Step 1: 初始化 git 仓库并配置身份**

```bash
cd /root/workspace/olaiya
git init
git config user.name "olaiya"
git config user.email "olaiya@example.com"
```

- [ ] **Step 2: 写 .gitignore、requirements.txt、pytest.ini 与包骨架**

`/root/workspace/olaiya/.gitignore`：

```gitignore
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.db
.env
data/raw/
```

`/root/workspace/olaiya/requirements.txt`：

```text
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
fastapi>=0.110
uvicorn>=0.29
pytest>=8.0
httpx>=0.27
```

`/root/workspace/olaiya/pytest.ini`：

```ini
[pytest]
pythonpath = backend
testpaths = backend/tests
addopts = -v
```

```bash
cd /root/workspace/olaiya
mkdir -p backend/app/models backend/tests data/raw data/seed data/loaders
touch backend/app/__init__.py backend/tests/__init__.py
```

- [ ] **Step 3: 创建 venv 并安装依赖**

```bash
cd /root/workspace/olaiya
python3 -m venv .venv
.venv/bin/python -m pip install -q -r requirements.txt
```

预期：无报错；`.venv/bin/python -c "import sqlalchemy, fastapi, pytest; print(sqlalchemy.__version__)"` 打印 2.x 版本号。

- [ ] **Step 4: pytest 冒烟（此时无测试，确认收集正常）**

```bash
cd /root/workspace/olaiya
.venv/bin/python -m pytest
```

预期：退出码 5（no tests ran）即正常，不能是报错。

- [ ] **Step 5: Commit**

```bash
cd /root/workspace/olaiya
git add .gitignore requirements.txt pytest.ini backend data
git commit -m "chore: 初始化仓库骨架与开发环境"
```

---

### Task 2: 配置模块（config）

**Files:**
- Create: `/root/workspace/olaiya/backend/app/config.py`
- Test: `/root/workspace/olaiya/backend/tests/test_config.py`

**Interfaces:**
- Consumes: 无
- Produces: `app.config.settings`（单例），字段 `database_url: str`，默认 `"sqlite:///./cfz.db"`；环境变量前缀 `CFZ_`（如 `CFZ_DATABASE_URL` 可覆盖）

- [ ] **Step 1: 写失败测试**

`/root/workspace/olaiya/backend/tests/test_config.py`：

```python
import importlib

from app import config as config_module


def test_default_database_url_is_sqlite():
    assert config_module.settings.database_url.startswith("sqlite")


def test_env_override(monkeypatch):
    monkeypatch.setenv("CFZ_DATABASE_URL", "sqlite:////tmp/override.db")
    importlib.reload(config_module)
    try:
        assert config_module.settings.database_url == "sqlite:////tmp/override.db"
    finally:
        monkeypatch.undo()
        importlib.reload(config_module)
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /root/workspace/olaiya
.venv/bin/python -m pytest backend/tests/test_config.py
```

预期：FAIL，`ModuleNotFoundError: No module named 'app.config'`。

注意：本任务执行时 conftest.py（Task 3 才创建）尚不存在；若 Task 3 已完成，conftest 会把 `CFZ_DATABASE_URL` 设为临时文件路径，与第一个断言不冲突（仍为 sqlite 开头）。

- [ ] **Step 3: 实现 config.py**

`/root/workspace/olaiya/backend/app/config.py`：

```python
"""全局配置。环境变量前缀 CFZ_，例如 CFZ_DATABASE_URL 覆盖数据库地址。"""

from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_prefix="CFZ_", env_file=".env", extra="ignore")

    # 开发期默认 SQLite 文件库；切换 PostgreSQL 只需改环境变量
    database_url: str = "sqlite:///./cfz.db"


settings = Settings()
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd /root/workspace/olaiya
.venv/bin/python -m pytest backend/tests/test_config.py
```

预期：2 passed。

- [ ] **Step 5: Commit**

```bash
cd /root/workspace/olaiya
git add backend/app/config.py backend/tests/test_config.py
git commit -m "feat: 新增全局配置模块"
```

---

### Task 3: 数据库连接（db）与测试基座

**Files:**
- Create: `/root/workspace/olaiya/backend/app/db.py`
- Create: `/root/workspace/olaiya/backend/app/models/__init__.py`
- Create: `/root/workspace/olaiya/backend/tests/conftest.py`
- Test: `/root/workspace/olaiya/backend/tests/test_db.py`

**Interfaces:**
- Consumes: `app.config.settings.database_url`
- Produces:
  - `app.db.Base`（DeclarativeBase，所有模型的基类）
  - `app.db.engine`、`app.db.SessionLocal`（sessionmaker）
  - `app.db.init_db() -> None`（`Base.metadata.create_all`，内部延迟导入 models）
  - conftest 提供 pytest fixture `session`：每个测试用独立临时文件库，建空表后 yield 一个 Session，测试结束关闭

- [ ] **Step 1: 写失败测试与 conftest**

`/root/workspace/olaiya/backend/tests/conftest.py`（必须先于任何 app 导入设置数据库地址，本文件 pytest 最先加载）：

```python
import os
import tempfile

# 必须在导入 app 之前设置：测试用独立临时文件库，避免污染开发库
_db_fd, _db_path = tempfile.mkstemp(suffix=".db")
os.close(_db_fd)
os.environ["CFZ_DATABASE_URL"] = f"sqlite:///{_db_path}"

import pytest

from app.db import Base, SessionLocal, engine, init_db


@pytest.fixture()
def session():
    """每个测试用例得到一张空表环境。"""
    Base.metadata.drop_all(engine)
    init_db()
    s = SessionLocal()
    try:
        yield s
    finally:
        s.close()
```

`/root/workspace/olaiya/backend/tests/test_db.py`：

```python
from sqlalchemy import inspect

from app.db import engine


def test_init_db_creates_tables(session):
    # init_db 已由 fixture 调用；models 注册后这些表必须存在
    tables = set(inspect(engine).get_table_names())
    assert {"evidence", "ingredients", "efficacy_assertions", "products", "product_ingredients", "price_points"} <= tables
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /root/workspace/olaiya
.venv/bin/python -m pytest backend/tests/test_db.py
```

预期：FAIL，`ModuleNotFoundError: No module named 'app.db'`。

- [ ] **Step 3: 实现 db.py 与 models 包**

`/root/workspace/olaiya/backend/app/db.py`：

```python
"""数据库连接。开发期 SQLite；换 PostgreSQL 只改 CFZ_DATABASE_URL。"""

from sqlalchemy import create_engine
from sqlalchemy.orm import DeclarativeBase, sessionmaker

from .config import settings


class Base(DeclarativeBase):
    pass


_connect_args = {"check_same_thread": False} if settings.database_url.startswith("sqlite") else {}
engine = create_engine(settings.database_url, connect_args=_connect_args)
SessionLocal = sessionmaker(bind=engine, autoflush=False, expire_on_commit=False)


def init_db() -> None:
    """建表。延迟导入 models 以完成表注册，避免循环导入。"""
    from . import models  # noqa: F401

    Base.metadata.create_all(engine)
```

`/root/workspace/olaiya/backend/app/models/__init__.py`（Task 4-6 会逐个实现被导入的模块；本步先建文件、内容先放空，Step 4 暂时跑不过属预期，Task 4-6 完成后此测试自然转绿——执行到 Task 6 结束前不要提交本测试的通过状态）：

```python
"""模型包。被 init_db 延迟导入以完成表注册。"""

from . import evidence, ingredient, product  # noqa: F401
```

注意：上一步要求 models/__init__.py 导入三个模型模块，而它们还不存在。为保持 TDD 节奏，本任务先把 `__init__.py` 写成**空文件**，`test_db.py` 暂时保持失败；在 Task 6 的 Step 4 中再把上面这行导入写入并确认 `test_db.py` 转绿。

- [ ] **Step 4: 跑测试确认当前状态**

```bash
cd /root/workspace/olaiya
.venv/bin/python -m pytest backend/tests/test_db.py
```

预期：`test_init_db_creates_tables` FAIL（表集合为空断言失败），证明 fixture 与建表链路工作正常。

- [ ] **Step 5: Commit**

```bash
cd /root/workspace/olaiya
git add backend/app/db.py backend/app/models/__init__.py backend/tests/conftest.py backend/tests/test_db.py
git commit -m "feat: 数据库连接与 pytest 测试基座"
```

---

### Task 4: Evidence 模型（证据）

**Files:**
- Create: `/root/workspace/olaiya/backend/app/models/evidence.py`
- Test: `/root/workspace/olaiya/backend/tests/test_evidence.py`

**Interfaces:**
- Consumes: `app.db.Base`
- Produces:
  - `EvidenceType` 枚举：`PAPER / PATENT / REGULATION / WHITE_PAPER`（值为 `"paper"/"patent"/"regulation"/"white_paper"`）
  - `Evidence` 表 `evidence`：`id:int pk`、`type:EvidenceType`、`title:str(500)`、`source:str(300)`、`year:int|None`、`url:str|None`、`excerpt:str|None`

- [ ] **Step 1: 写失败测试**

`/root/workspace/olaiya/backend/tests/test_evidence.py`：

```python
from app.models.evidence import Evidence, EvidenceType


def test_create_paper_evidence(session):
    e = Evidence(
        type=EvidenceType.PAPER,
        title="Improvement of naturally aged skin with vitamin A (retinol)",
        source="Archives of Dermatology, 2007;143(5):606-612",
        year=2007,
        url="https://pubmed.ncbi.nlm.nih.gov/17515510/",
        excerpt="Topical 0.4% retinol improved fine wrinkling.",
    )
    session.add(e)
    session.commit()
    got = session.query(Evidence).filter_by(title=e.title).one()
    assert got.type == EvidenceType.PAPER
    assert got.year == 2007


def test_all_four_evidence_types(session):
    for t in EvidenceType:
        session.add(Evidence(type=t, title=f"t-{t.value}", source="s"))
    session.commit()
    assert session.query(Evidence).count() == 4
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /root/workspace/olaiya
.venv/bin/python -m pytest backend/tests/test_evidence.py
```

预期：FAIL，`ModuleNotFoundError: No module named 'app.models.evidence'`。

- [ ] **Step 3: 实现 evidence.py**

`/root/workspace/olaiya/backend/app/models/evidence.py`：

```python
"""证据：论文 / 专利 / 法规 / 白皮书。每条功效断言必须挂一条证据。"""

import enum

from sqlalchemy import Enum as SAEnum
from sqlalchemy import Integer, String
from sqlalchemy.orm import Mapped, mapped_column

from ..db import Base


class EvidenceType(str, enum.Enum):
    PAPER = "paper"
    PATENT = "patent"
    REGULATION = "regulation"
    WHITE_PAPER = "white_paper"


class Evidence(Base):
    __tablename__ = "evidence"

    id: Mapped[int] = mapped_column(primary_key=True)
    type: Mapped[EvidenceType] = mapped_column(SAEnum(EvidenceType), index=True)
    title: Mapped[str] = mapped_column(String(500))
    source: Mapped[str] = mapped_column(String(300))  # 期刊/专利局/法规发布方
    year: Mapped[int | None] = mapped_column(Integer, nullable=True)
    url: Mapped[str | None] = mapped_column(String(1000), nullable=True)
    excerpt: Mapped[str | None] = mapped_column(String(2000), nullable=True)  # 支撑结论的原文摘录
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd /root/workspace/olaiya
.venv/bin/python -m pytest backend/tests/test_evidence.py
```

预期：2 passed。

- [ ] **Step 5: Commit**

```bash
cd /root/workspace/olaiya
git add backend/app/models/evidence.py backend/tests/test_evidence.py
git commit -m "feat: 新增证据模型"
```

---

### Task 5: Ingredient 与 EfficacyAssertion 模型（数据铁律：无证据不入库）

**Files:**
- Create: `/root/workspace/olaiya/backend/app/models/ingredient.py`
- Test: `/root/workspace/olaiya/backend/tests/test_ingredient.py`

**Interfaces:**
- Consumes: `app.db.Base`、`app.models.evidence.Evidence`
- Produces:
  - `Ingredient` 表 `ingredients`：`id`、`inci_name:str(200) unique`、`cn_name:str(200)`、`cas_no:str|None`、先验字段 `iecic_max_leave_on / iecic_max_rinse_off / legal_cap / cir_conc_low / cir_conc_high / sccs_limit: float|None`
  - `EfficacyAssertion` 表 `efficacy_assertions`：`id`、`ingredient_id fk->ingredients.id`、`efficacy:str(100)`、`evidence_id fk->evidence.id NOT NULL`、`effective_conc_low / effective_conc_high: float|None`、`note:str|None`；relationship：`.ingredient`、`.evidence`

- [ ] **Step 1: 写失败测试**

`/root/workspace/olaiya/backend/tests/test_ingredient.py`：

```python
import pytest
from sqlalchemy.exc import IntegrityError

from app.models.evidence import Evidence, EvidenceType
from app.models.ingredient import EfficacyAssertion, Ingredient


def _make_evidence(session) -> Evidence:
    e = Evidence(type=EvidenceType.PAPER, title="某论文", source="某期刊", year=2020)
    session.add(e)
    session.commit()
    return e


def test_ingredient_with_priors(session):
    ing = Ingredient(inci_name="NIACINAMIDE", cn_name="烟酰胺", cas_no="98-92-0",
                     iecic_max_leave_on=20.0, legal_cap=None)
    session.add(ing)
    session.commit()
    got = session.query(Ingredient).filter_by(inci_name="NIACINAMIDE").one()
    assert got.cn_name == "烟酰胺"
    assert got.iecic_max_leave_on == 20.0


def test_assertion_requires_evidence(session):
    ing = Ingredient(inci_name="RETINOL", cn_name="视黄醇")
    session.add(ing)
    session.commit()
    # 铁律验证：evidence_id 为空必须入库失败
    session.add(EfficacyAssertion(ingredient_id=ing.id, efficacy="抗皱", evidence_id=None))
    with pytest.raises(IntegrityError):
        session.commit()
    session.rollback()


def test_assertion_links_evidence(session):
    ing = Ingredient(inci_name="RETINOL", cn_name="视黄醇")
    ev = _make_evidence(session)
    session.add(ing)
    session.commit()
    a = EfficacyAssertion(ingredient_id=ing.id, efficacy="抗皱", evidence_id=ev.id,
                          effective_conc_low=0.1, effective_conc_high=0.4)
    session.add(a)
    session.commit()
    got = session.query(EfficacyAssertion).one()
    assert got.evidence.title == "某论文"
    assert got.ingredient.inci_name == "RETINOL"
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /root/workspace/olaiya
.venv/bin/python -m pytest backend/tests/test_ingredient.py
```

预期：FAIL，`ModuleNotFoundError: No module named 'app.models.ingredient'`。

- [ ] **Step 3: 实现 ingredient.py**

`/root/workspace/olaiya/backend/app/models/ingredient.py`：

```python
"""成分与功效断言。铁律：功效断言的 evidence_id 不可为空 —— 无证据不入库。"""

from sqlalchemy import Float, ForeignKey, String, Text
from sqlalchemy.orm import Mapped, mapped_column, relationship

from ..db import Base


class Ingredient(Base):
    __tablename__ = "ingredients"

    id: Mapped[int] = mapped_column(primary_key=True)
    inci_name: Mapped[str] = mapped_column(String(200), unique=True, index=True)
    cn_name: Mapped[str] = mapped_column(String(200), index=True)
    cas_no: Mapped[str | None] = mapped_column(String(50), nullable=True)
    # —— 浓度先验（单位 %），来自 IECIC2021 / 安全技术规范 / CIR / SCCS，未知留空 ——
    iecic_max_leave_on: Mapped[float | None] = mapped_column(Float, nullable=True)   # IECIC 最高历史使用量·驻留类
    iecic_max_rinse_off: Mapped[float | None] = mapped_column(Float, nullable=True)  # 淋洗类
    legal_cap: Mapped[float | None] = mapped_column(Float, nullable=True)            # 安全技术规范法定上限
    cir_conc_low: Mapped[float | None] = mapped_column(Float, nullable=True)         # CIR 使用浓度区间
    cir_conc_high: Mapped[float | None] = mapped_column(Float, nullable=True)
    sccs_limit: Mapped[float | None] = mapped_column(Float, nullable=True)           # SCCS 安全上限


class EfficacyAssertion(Base):
    __tablename__ = "efficacy_assertions"

    id: Mapped[int] = mapped_column(primary_key=True)
    ingredient_id: Mapped[int] = mapped_column(ForeignKey("ingredients.id"), index=True)
    efficacy: Mapped[str] = mapped_column(String(100), index=True)  # 美白/保湿/抗皱...
    evidence_id: Mapped[int] = mapped_column(ForeignKey("evidence.id"), nullable=False)  # 铁律
    # 文献起效浓度区间（%），未知留空；剂量判定的基准
    effective_conc_low: Mapped[float | None] = mapped_column(Float, nullable=True)
    effective_conc_high: Mapped[float | None] = mapped_column(Float, nullable=True)
    note: Mapped[str | None] = mapped_column(Text, nullable=True)

    ingredient = relationship("Ingredient")
    evidence = relationship("Evidence")
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd /root/workspace/olaiya
.venv/bin/python -m pytest backend/tests/test_ingredient.py
```

预期：3 passed。

- [ ] **Step 5: Commit**

```bash
cd /root/workspace/olaiya
git add backend/app/models/ingredient.py backend/tests/test_ingredient.py
git commit -m "feat: 成分与功效断言模型，强制断言挂证据"
```

---

### Task 6: Product 相关模型

**Files:**
- Create: `/root/workspace/olaiya/backend/app/models/product.py`
- Modify: `/root/workspace/olaiya/backend/app/models/__init__.py`（写入正式导入）
- Test: `/root/workspace/olaiya/backend/tests/test_product.py`

**Interfaces:**
- Consumes: `app.db.Base`、`app.models.ingredient.Ingredient`
- Produces:
  - `Product` 表 `products`：`id`、`name:str(300)`、`brand:str(200)`、`category:str(100)|None`、`nmpa_id:str(100)|None unique`、`price_current:float|None`、`note:str|None`
  - `ProductIngredient` 表 `product_ingredients`：`id`、`product_id fk`、`ingredient_id fk`、`position:int`（>0.1% 段内 1-based 位次）、`is_trace:bool=False`（是否"其他微量成分"段）、`conc_low / conc_high / conc_confidence: float|None`（推断结果，计划 02 填充）；relationship：`.product`、`.ingredient`
  - `PricePoint` 表 `price_points`：`id`、`product_id fk`、`date:Date`、`price:float`、`source:str(200)`、`is_manual:bool=True`
  - `app.models` 包完成注册（`init_db` 依赖）

- [ ] **Step 1: 写失败测试**

`/root/workspace/olaiya/backend/tests/test_product.py`：

```python
import datetime

from app.models.ingredient import Ingredient
from app.models.product import PricePoint, Product, ProductIngredient


def _product_with_ingredients(session):
    p = Product(name="测试精华", brand="测试牌", category="精华", price_current=199.0)
    water = Ingredient(inci_name="WATER", cn_name="水")
    nia = Ingredient(inci_name="NIACINAMIDE", cn_name="烟酰胺")
    pe = Ingredient(inci_name="PHENOXYETHANOL", cn_name="苯氧乙醇", legal_cap=1.0)
    session.add_all([p, water, nia, pe])
    session.commit()
    session.add_all([
        ProductIngredient(product_id=p.id, ingredient_id=water.id, position=1),
        ProductIngredient(product_id=p.id, ingredient_id=nia.id, position=2),
        ProductIngredient(product_id=p.id, ingredient_id=pe.id, position=3),
    ])
    session.commit()
    return p


def test_product_ingredient_ordering(session):
    p = _product_with_ingredients(session)
    rows = (session.query(ProductIngredient)
            .filter_by(product_id=p.id)
            .order_by(ProductIngredient.position).all())
    assert [r.ingredient.inci_name for r in rows] == ["WATER", "NIACINAMIDE", "PHENOXYETHANOL"]
    assert all(r.is_trace is False for r in rows)
    assert rows[0].conc_low is None  # 推断字段默认为空，等待计划 02


def test_price_point(session):
    p = _product_with_ingredients(session)
    pp = PricePoint(product_id=p.id, date=datetime.date(2026, 7, 26),
                    price=199.0, source="人工采样", is_manual=True)
    session.add(pp)
    session.commit()
    got = session.query(PricePoint).one()
    assert got.price == 199.0 and got.is_manual is True
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /root/workspace/olaiya
.venv/bin/python -m pytest backend/tests/test_product.py
```

预期：FAIL，`ModuleNotFoundError: No module named 'app.models.product'`。

- [ ] **Step 3: 实现 product.py 并补全 models 注册**

`/root/workspace/olaiya/backend/app/models/product.py`：

```python
"""产品档案：产品-成分有序关联（位次即备案成分表降序）、价格采样点。"""

import datetime

from sqlalchemy import Date, Float, ForeignKey, String, Text
from sqlalchemy.orm import Mapped, mapped_column, relationship

from ..db import Base


class Product(Base):
    __tablename__ = "products"

    id: Mapped[int] = mapped_column(primary_key=True)
    name: Mapped[str] = mapped_column(String(300), index=True)
    brand: Mapped[str] = mapped_column(String(200), index=True)
    category: Mapped[str | None] = mapped_column(String(100), nullable=True)
    nmpa_id: Mapped[str | None] = mapped_column(String(100), unique=True, nullable=True)  # 备案编号
    price_current: Mapped[float | None] = mapped_column(Float, nullable=True)  # 人民币元，人工采样
    note: Mapped[str | None] = mapped_column(Text, nullable=True)  # 如"品牌公布 15%VC+1%VE"


class ProductIngredient(Base):
    __tablename__ = "product_ingredients"

    id: Mapped[int] = mapped_column(primary_key=True)
    product_id: Mapped[int] = mapped_column(ForeignKey("products.id"), index=True)
    ingredient_id: Mapped[int] = mapped_column(ForeignKey("ingredients.id"), index=True)
    position: Mapped[int] = mapped_column()  # 成分表位次（1-based，降序）；微量段内顺序无意义
    is_trace: Mapped[bool] = mapped_column(default=False)  # 是否"其他微量成分"段（≤0.1%）
    # —— 浓度区间推断结果（%），由计划 02 的推断引擎填充 ——
    conc_low: Mapped[float | None] = mapped_column(Float, nullable=True)
    conc_high: Mapped[float | None] = mapped_column(Float, nullable=True)
    conc_confidence: Mapped[float | None] = mapped_column(Float, nullable=True)

    product = relationship("Product")
    ingredient = relationship("Ingredient")


class PricePoint(Base):
    __tablename__ = "price_points"

    id: Mapped[int] = mapped_column(primary_key=True)
    product_id: Mapped[int] = mapped_column(ForeignKey("products.id"), index=True)
    date: Mapped[datetime.date] = mapped_column(Date)
    price: Mapped[float] = mapped_column(Float)
    source: Mapped[str] = mapped_column(String(200))  # 人工采样/联盟API
    is_manual: Mapped[bool] = mapped_column(default=True)
```

`/root/workspace/olaiya/backend/app/models/__init__.py`（整文件替换为）：

```python
"""模型包。被 init_db 延迟导入以完成表注册。"""

from . import evidence, ingredient, product  # noqa: F401
```

- [ ] **Step 4: 跑测试确认通过（含 Task 3 遗留的 test_db）**

```bash
cd /root/workspace/olaiya
.venv/bin/python -m pytest backend/tests/test_product.py backend/tests/test_db.py
```

预期：两个文件全部 passed（`test_init_db_creates_tables` 此刻转绿）。

- [ ] **Step 5: Commit**

```bash
cd /root/workspace/olaiya
git add backend/app/models/product.py backend/app/models/__init__.py backend/tests/test_product.py
git commit -m "feat: 产品/产品成分/价格模型，完成全表注册"
```

---

### Task 7: 种子数据与幂等加载器

**Files:**
- Create: `/root/workspace/olaiya/data/__init__.py`（空）、`/root/workspace/olaiya/data/loaders/__init__.py`（空）
- Create: `/root/workspace/olaiya/data/seed/seed_data.json`
- Create: `/root/workspace/olaiya/data/loaders/seed_loader.py`
- Test: `/root/workspace/olaiya/backend/tests/test_seed_loader.py`

**Interfaces:**
- Consumes: 全部模型（Task 4-6）
- Produces:
  - `data.loaders.seed_loader.SEED_PATH: Path`（默认指向 `data/seed/seed_data.json`）
  - `data.loaders.seed_loader.load_seed(session, path=SEED_PATH) -> None`：幂等（重复执行不产生重复行）；产品中未登记的成分自动建 stub（`cn_name=inci_name`，先验字段留空）
  - CLI：仓库根目录执行 `PYTHONPATH=backend .venv/bin/python -m data.loaders.seed_loader` 将种子写入开发库并打印计数
  - 种子内含 5 个成分（烟酰胺/视黄醇/抗坏血酸/透明质酸/苯氧乙醇）、5 条证据、4 条功效断言、2 个产品（修丽可 CE、The Ordinary 烟酰胺，均为品牌公布浓度的校准锚点）

- [ ] **Step 1: 写失败测试**

`/root/workspace/olaiya/backend/tests/test_seed_loader.py`：

```python
from data.loaders.seed_loader import load_seed
from app.models.evidence import Evidence
from app.models.ingredient import EfficacyAssertion, Ingredient
from app.models.product import Product, ProductIngredient


def test_load_seed_counts(session):
    load_seed(session)
    session.commit()
    assert session.query(Evidence).count() == 5
    # 5 个全字段成分 + 产品成分表自动补的 stub
    assert session.query(Ingredient).filter_by(inci_name="NIACINAMIDE").one().cn_name == "烟酰胺"
    assert session.query(Ingredient).filter_by(inci_name="ZINC PCA").count() == 1  # stub
    assert session.query(EfficacyAssertion).count() == 4
    assert session.query(Product).count() == 2
    ce = session.query(Product).filter_by(brand="修丽可 SkinCeuticals").one()
    first = (session.query(ProductIngredient).filter_by(product_id=ce.id)
             .order_by(ProductIngredient.position).first())
    assert first.ingredient.inci_name == "WATER" and first.position == 1


def test_load_seed_idempotent(session):
    load_seed(session)
    session.commit()
    load_seed(session)
    session.commit()
    assert session.query(Evidence).count() == 5
    assert session.query(Product).count() == 2
    assert session.query(EfficacyAssertion).count() == 4


def test_every_assertion_has_evidence(session):
    load_seed(session)
    session.commit()
    for a in session.query(EfficacyAssertion).all():
        assert a.evidence is not None and a.evidence.url
```

注意：`pythonpath = backend` 已在 pytest.ini 配置，仓库根目录本身是 sys.path 根，`data` 包可被直接导入。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /root/workspace/olaiya
touch data/__init__.py data/loaders/__init__.py
.venv/bin/python -m pytest backend/tests/test_seed_loader.py
```

预期：FAIL，`ModuleNotFoundError: No module named 'data.loaders.seed_loader'`。

- [ ] **Step 3: 写种子数据与加载器**

`/root/workspace/olaiya/data/seed/seed_data.json`（文献均为真实可查条目；浓度数字为文献/法规口径）：

```json
{
  "evidence": [
    {
      "type": "paper",
      "title": "The effect of niacinamide on reducing cutaneous pigmentation and suppression of melanosome transfer",
      "source": "British Journal of Dermatology, 2002;147(1):20-31",
      "year": 2002,
      "url": "https://pubmed.ncbi.nlm.nih.gov/12100180/",
      "excerpt": "5% niacinamide moisturizer significantly decreased hyperpigmentation; niacinamide inhibited melanosome transfer from melanocytes to keratinocytes in vitro."
    },
    {
      "type": "paper",
      "title": "Improvement of naturally aged skin with vitamin A (retinol)",
      "source": "Archives of Dermatology, 2007;143(5):606-612",
      "year": 2007,
      "url": "https://pubmed.ncbi.nlm.nih.gov/17515510/",
      "excerpt": "Topical 0.4% retinol applied up to 12 weeks significantly improved fine wrinkling associated with natural aging."
    },
    {
      "type": "paper",
      "title": "Topical L-ascorbic acid: percutaneous absorption studies",
      "source": "Dermatologic Surgery, 2001;27(2):137-142",
      "year": 2001,
      "url": "https://pubmed.ncbi.nlm.nih.gov/11207686/",
      "excerpt": "L-ascorbic acid must be formulated at pH below 3.5 for optimal percutaneous absorption; 20% was the maximum concentration for skin absorption."
    },
    {
      "type": "paper",
      "title": "Efficacy of cream-based novel formulations of hyaluronic acid of different molecular weights in anti-wrinkle treatment",
      "source": "Journal of Drugs in Dermatology, 2011;10(9):990-1000",
      "year": 2011,
      "url": "https://pubmed.ncbi.nlm.nih.gov/22052267/",
      "excerpt": "Creams containing 0.1% hyaluronic acid of various molecular weights significantly improved skin hydration and elasticity."
    },
    {
      "type": "regulation",
      "title": "化妆品安全技术规范（2015年版）表4 化妆品准用防腐剂",
      "source": "国家食品药品监督管理总局",
      "year": 2015,
      "url": "https://www.nmpa.gov.cn/directory/web/nmpa/images/MjAxNcTqtdoyNji6xbmruOa4vbz+LnBkZg==.pdf",
      "excerpt": "苯氧乙醇（Phenoxyethanol）为化妆品准用防腐剂，最大允许使用浓度 1.0%。"
    }
  ],
  "ingredients": [
    {
      "inci_name": "NIACINAMIDE", "cn_name": "烟酰胺", "cas_no": "98-92-0",
      "assertions": [
        {"efficacy": "美白（抑制黑素小体转运）",
         "evidence_title": "The effect of niacinamide on reducing cutaneous pigmentation and suppression of melanosome transfer",
         "effective_conc_low": 2.0, "effective_conc_high": 5.0,
         "note": "临床试验常用浓度 2%-5%"}
      ]
    },
    {
      "inci_name": "RETINOL", "cn_name": "视黄醇", "cas_no": "68-26-8",
      "assertions": [
        {"efficacy": "抗皱（改善自然老化皮肤细纹）",
         "evidence_title": "Improvement of naturally aged skin with vitamin A (retinol)",
         "effective_conc_low": 0.1, "effective_conc_high": 0.4,
         "note": "研究浓度 0.4%；使用建议从低浓度起步"}
      ]
    },
    {
      "inci_name": "ASCORBIC ACID", "cn_name": "抗坏血酸（维生素C）", "cas_no": "50-81-7",
      "assertions": [
        {"efficacy": "抗氧化/美白提亮",
         "evidence_title": "Topical L-ascorbic acid: percutaneous absorption studies",
         "effective_conc_low": 8.0, "effective_conc_high": 20.0,
         "note": "透皮吸收需配方 pH<3.5；浓度超过 20% 吸收率不再提升"}
      ]
    },
    {
      "inci_name": "HYALURONIC ACID", "cn_name": "透明质酸", "cas_no": "9004-61-9",
      "assertions": [
        {"efficacy": "保湿",
         "evidence_title": "Efficacy of cream-based novel formulations of hyaluronic acid of different molecular weights in anti-wrinkle treatment",
         "effective_conc_low": 0.1, "effective_conc_high": 0.5,
         "note": "0.1% 即可显著改善皮肤水合"}
      ]
    },
    {
      "inci_name": "PHENOXYETHANOL", "cn_name": "苯氧乙醇", "cas_no": "122-99-6",
      "legal_cap": 1.0,
      "assertions": []
    }
  ],
  "products": [
    {
      "name": "维生素CE复合修护精华液（CE Ferulic）",
      "brand": "修丽可 SkinCeuticals",
      "category": "精华",
      "nmpa_id": null,
      "price_current": 1490.0,
      "note": "校准锚点：品牌公布 15% 抗坏血酸 + 1% 生育酚 + 0.5% 阿魏酸",
      "ingredients": ["WATER", "ETHOXYDIGLYCOL", "ASCORBIC ACID", "GLYCERIN", "PROPYLENE GLYCOL", "LAURETH-23", "PHENOXYETHANOL", "TOCOPHEROL", "TRIETHANOLAMINE", "FERULIC ACID", "PANTHENOL", "SODIUM HYALURONATE"],
      "trace_ingredients": []
    },
    {
      "name": "烟酰胺10%+锌1%精华（Niacinamide 10% + Zinc 1%）",
      "brand": "The Ordinary",
      "category": "精华",
      "nmpa_id": null,
      "price_current": 89.0,
      "note": "校准锚点：品牌公布 10% 烟酰胺 + 1% PCA锌",
      "ingredients": ["WATER", "NIACINAMIDE", "PENTYLENE GLYCOL", "ZINC PCA", "DIMETHYL ISOSORBIDE", "TAMARINDUS INDICA SEED GUM", "XANTHAN GUM", "ISOCETETH-20", "ETHOXYDIGLYCOL", "PHENOXYETHANOL", "CHLORPHENESIN"],
      "trace_ingredients": []
    }
  ]
}
```

`/root/workspace/olaiya/data/loaders/seed_loader.py`：

```python
"""种子数据加载器：幂等写入证据/成分/断言/产品。

产品中出现的未登记成分自动建 stub（cn_name 暂用 INCI 名，先验留空）。
CLI：仓库根目录执行  PYTHONPATH=backend .venv/bin/python -m data.loaders.seed_loader
"""

import json
from pathlib import Path

from sqlalchemy.orm import Session

from app.db import SessionLocal, init_db
from app.models.evidence import Evidence, EvidenceType
from app.models.ingredient import EfficacyAssertion, Ingredient
from app.models.product import Product, ProductIngredient

SEED_PATH = Path(__file__).resolve().parents[1] / "seed" / "seed_data.json"


def _load_evidence(session: Session, items: list[dict]) -> dict[str, Evidence]:
    """按 title 幂等 upsert，返回 title -> Evidence 映射。"""
    by_title: dict[str, Evidence] = {}
    for item in items:
        ev = session.query(Evidence).filter_by(title=item["title"]).one_or_none()
        if ev is None:
            ev = Evidence(
                type=EvidenceType(item["type"]),
                title=item["title"],
                source=item["source"],
                year=item.get("year"),
                url=item.get("url"),
                excerpt=item.get("excerpt"),
            )
            session.add(ev)
            session.flush()
        by_title[item["title"]] = ev
    return by_title


def _get_or_create_ingredient(session: Session, inci_name: str, cn_name: str | None = None) -> Ingredient:
    ing = session.query(Ingredient).filter_by(inci_name=inci_name).one_or_none()
    if ing is None:
        ing = Ingredient(inci_name=inci_name, cn_name=cn_name or inci_name)
        session.add(ing)
        session.flush()
    return ing


def _load_ingredients(session: Session, items: list[dict], evidence_by_title: dict[str, Evidence]) -> None:
    prior_fields = ("iecic_max_leave_on", "iecic_max_rinse_off", "legal_cap",
                    "cir_conc_low", "cir_conc_high", "sccs_limit")
    for item in items:
        ing = _get_or_create_ingredient(session, item["inci_name"], item["cn_name"])
        ing.cas_no = item.get("cas_no")
        for f in prior_fields:
            if item.get(f) is not None:
                setattr(ing, f, item[f])
        for a in item.get("assertions", []):
            ev = evidence_by_title[a["evidence_title"]]
            exists = (session.query(EfficacyAssertion)
                      .filter_by(ingredient_id=ing.id, efficacy=a["efficacy"], evidence_id=ev.id)
                      .one_or_none())
            if exists is None:
                session.add(EfficacyAssertion(
                    ingredient_id=ing.id,
                    efficacy=a["efficacy"],
                    evidence_id=ev.id,
                    effective_conc_low=a.get("effective_conc_low"),
                    effective_conc_high=a.get("effective_conc_high"),
                    note=a.get("note"),
                ))


def _load_products(session: Session, items: list[dict]) -> None:
    for item in items:
        product = (session.query(Product)
                   .filter_by(name=item["name"], brand=item["brand"]).one_or_none())
        if product is None:
            product = Product(
                name=item["name"], brand=item["brand"],
                category=item.get("category"), nmpa_id=item.get("nmpa_id"),
                price_current=item.get("price_current"), note=item.get("note"),
            )
            session.add(product)
            session.flush()
        existing = session.query(ProductIngredient).filter_by(product_id=product.id).count()
        if existing:  # 已有成分表则跳过，保证幂等
            continue
        position = 0
        for inci in item.get("ingredients", []):
            position += 1
            ing = _get_or_create_ingredient(session, inci)
            session.add(ProductIngredient(product_id=product.id, ingredient_id=ing.id,
                                          position=position, is_trace=False))
        for inci in item.get("trace_ingredients", []):
            position += 1
            ing = _get_or_create_ingredient(session, inci)
            session.add(ProductIngredient(product_id=product.id, ingredient_id=ing.id,
                                          position=position, is_trace=True))


def load_seed(session: Session, path: Path = SEED_PATH) -> None:
    data = json.loads(Path(path).read_text(encoding="utf-8"))
    evidence_by_title = _load_evidence(session, data["evidence"])
    _load_ingredients(session, data["ingredients"], evidence_by_title)
    _load_products(session, data["products"])


def main() -> None:
    init_db()
    with SessionLocal() as s:
        load_seed(s)
        s.commit()
        print(f"evidence={s.query(Evidence).count()} "
              f"ingredients={s.query(Ingredient).count()} "
              f"assertions={s.query(EfficacyAssertion).count()} "
              f"products={s.query(Product).count()}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd /root/workspace/olaiya
.venv/bin/python -m pytest backend/tests/test_seed_loader.py
```

预期：3 passed。

- [ ] **Step 5: 用 CLI 写开发库并验证**

```bash
cd /root/workspace/olaiya
PYTHONPATH=backend .venv/bin/python -m data.loaders.seed_loader
```

预期输出（ingredients 数 = 5 全字段 + 产品一 10 个 stub + 产品二 7 个 stub = 22）：

```text
evidence=5 ingredients=22 assertions=4 products=2
```

- [ ] **Step 6: Commit**

```bash
cd /root/workspace/olaiya
git add data backend/tests/test_seed_loader.py
git commit -m "feat: 真实种子数据与幂等加载器"
```

---

### Task 8: 最小只读 API 与启动联调

**Files:**
- Create: `/root/workspace/olaiya/backend/app/main.py`
- Test: `/root/workspace/olaiya/backend/tests/test_api.py`

**Interfaces:**
- Consumes: `app.db.SessionLocal/init_db`、模型、`data.loaders.seed_loader.load_seed`（测试用）
- Produces（后续计划依赖的 API 形态与依赖注入方式）：
  - `app.main.app`（FastAPI 实例）、`app.main.get_db`（依赖，测试用 `app.dependency_overrides` 覆盖）
  - `GET /api/ingredients?q=` → `[{id, inci_name, cn_name, cas_no}]`
  - `GET /api/ingredients/{id}` → `{id, inci_name, cn_name, cas_no, priors:{...}, assertions:[{efficacy, effective_conc_low, effective_conc_high, note, evidence:{id,type,title,source,year,url,excerpt}}]}`；不存在返回 404 `{"detail": "成分不存在"}`

- [ ] **Step 1: 写失败测试**

`/root/workspace/olaiya/backend/tests/test_api.py`：

```python
import pytest
from fastapi.testclient import TestClient

from app.main import app, get_db
from data.loaders.seed_loader import load_seed


@pytest.fixture()
def client(session):
    load_seed(session)
    session.commit()
    app.dependency_overrides[get_db] = lambda: session
    try:
        yield TestClient(app)
    finally:
        app.dependency_overrides.clear()


def test_list_ingredients(client):
    r = client.get("/api/ingredients")
    assert r.status_code == 200
    names = {i["inci_name"] for i in r.json()}
    assert "NIACINAMIDE" in names


def test_search_by_cn_name(client):
    r = client.get("/api/ingredients", params={"q": "烟酰"})
    assert r.status_code == 200
    assert [i["cn_name"] for i in r.json()] == ["烟酰胺"]


def test_ingredient_detail_has_evidence_chain(client):
    items = client.get("/api/ingredients", params={"q": "烟酰胺"}).json()
    r = client.get(f"/api/ingredients/{items[0]['id']}")
    assert r.status_code == 200
    body = r.json()
    assert body["assertions"], "种子里的烟酰胺必须有功效断言"
    a = body["assertions"][0]
    assert a["evidence"]["type"] == "paper"
    assert a["evidence"]["url"].startswith("https://pubmed.ncbi.nlm.nih.gov/")
    assert a["effective_conc_low"] == 2.0


def test_ingredient_not_found(client):
    r = client.get("/api/ingredients/999999")
    assert r.status_code == 404
    assert r.json()["detail"] == "成分不存在"
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /root/workspace/olaiya
.venv/bin/python -m pytest backend/tests/test_api.py
```

预期：FAIL，`ModuleNotFoundError: No module named 'app.main'`。

- [ ] **Step 3: 实现 main.py**

`/root/workspace/olaiya/backend/app/main.py`：

```python
"""成分真言 API。启动：仓库根目录
PYTHONPATH=backend .venv/bin/python -m uvicorn app.main:app --reload --port 8000
"""

from fastapi import Depends, FastAPI, HTTPException
from sqlalchemy import or_, select
from sqlalchemy.orm import Session

from .db import SessionLocal, init_db
from .models.ingredient import EfficacyAssertion, Ingredient

app = FastAPI(title="成分真言 API", version="0.1.0")


@app.on_event("startup")
def _startup() -> None:
    init_db()


def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()


def _assertion_dict(a: EfficacyAssertion) -> dict:
    ev = a.evidence
    return {
        "efficacy": a.efficacy,
        "effective_conc_low": a.effective_conc_low,
        "effective_conc_high": a.effective_conc_high,
        "note": a.note,
        "evidence": {
            "id": ev.id, "type": ev.type.value, "title": ev.title,
            "source": ev.source, "year": ev.year, "url": ev.url, "excerpt": ev.excerpt,
        },
    }


@app.get("/api/ingredients")
def list_ingredients(q: str | None = None, db: Session = Depends(get_db)):
    stmt = select(Ingredient).order_by(Ingredient.id)
    if q:
        like = f"%{q}%"
        stmt = select(Ingredient).where(
            or_(Ingredient.cn_name.like(like), Ingredient.inci_name.like(like))
        ).order_by(Ingredient.id)
    rows = db.execute(stmt).scalars().all()
    return [{"id": i.id, "inci_name": i.inci_name, "cn_name": i.cn_name, "cas_no": i.cas_no}
            for i in rows]


@app.get("/api/ingredients/{ingredient_id}")
def ingredient_detail(ingredient_id: int, db: Session = Depends(get_db)):
    ing = db.get(Ingredient, ingredient_id)
    if ing is None:
        raise HTTPException(status_code=404, detail="成分不存在")
    assertions = (db.query(EfficacyAssertion)
                  .filter_by(ingredient_id=ing.id)
                  .order_by(EfficacyAssertion.id).all())
    return {
        "id": ing.id,
        "inci_name": ing.inci_name,
        "cn_name": ing.cn_name,
        "cas_no": ing.cas_no,
        "priors": {
            "iecic_max_leave_on": ing.iecic_max_leave_on,
            "iecic_max_rinse_off": ing.iecic_max_rinse_off,
            "legal_cap": ing.legal_cap,
            "cir_conc_low": ing.cir_conc_low,
            "cir_conc_high": ing.cir_conc_high,
            "sccs_limit": ing.sccs_limit,
        },
        "assertions": [_assertion_dict(a) for a in assertions],
    }
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd /root/workspace/olaiya
.venv/bin/python -m pytest backend/tests/test_api.py
```

预期：4 passed。

- [ ] **Step 5: 全量回归 + 启动联调**

```bash
cd /root/workspace/olaiya
.venv/bin/python -m pytest
PYTHONPATH=backend .venv/bin/python -m uvicorn app.main:app --port 8000 &
sleep 3
curl -s "http://127.0.0.1:8000/api/ingredients?q=%E7%83%9F%E9%85%B0%E8%83%BA"
curl -s "http://127.0.0.1:8000/api/ingredients/1"
kill %1
```

预期：全量测试全绿；第一个 curl 返回含 `"cn_name":"烟酰胺"` 的列表；第二个 curl 返回含 `assertions` 与 `evidence` 的 JSON。

- [ ] **Step 6: Commit**

```bash
cd /root/workspace/olaiya
git add backend/app/main.py backend/tests/test_api.py
git commit -m "feat: 最小只读 API（成分列表与证据链详情）"
```

---

## 完成定义（Definition of Done）

- [ ] `.venv/bin/python -m pytest` 全绿（≥17 个用例）
- [ ] CLI 加载器输出 `evidence=5 ingredients=22 assertions=4 products=2`
- [ ] uvicorn 启动后两个 curl 返回符合接口约定的 JSON
- [ ] git log 有 8 个任务对应的提交，工作区干净
