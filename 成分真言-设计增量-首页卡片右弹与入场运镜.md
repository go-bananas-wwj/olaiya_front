# 设计增量计划：首页成分卡右弹 + 入场运镜动画

> 状态：待用户批准 ｜ 日期：2026-07-28 ｜ 范围：仅首页 Hero（lab/hero-demo 原型先行，验证后回主前端）
> 已定稿前提：珍珠贝母配色、童话/动漫卡通风、瓶器展台概念、V2 slogan「美，经得起逐滴核验」

---

## 改动一：成分介绍卡改为屏幕右侧弹出

### 交互规格

- **触发**：点击瓶内任一成分球（现有行为不变）；切换点击其他球时卡片内容原地切换（不重复弹入动画，只做内容淡切 200ms）。
- **位置**：`fixed`，右边缘 `right: 32px`，垂直居中（`top: 50%, translateY(-50%)`），宽 340px，最大高度 72vh（超出内部滚动，隐藏滚动条）。
- **入场动画**：从屏幕右侧滑入——`x: 110% → 0`，spring（stiffness 260, damping 26）或 cubic-bezier(0.22, 1, 0.36, 1) 480ms；同时轻微 `scale 0.96 → 1`、`opacity 0 → 1`。
- **退场动画**：反向滑出 380ms（点 ✕、点空白、或 Esc 关闭）。
- **遮挡处理**：卡片 z-index 在 3D 场景与所有 UI 之上；瓶区与卡片重叠处不做避让（卡片为浮层语义）；背景层加 8% 暗纱（pointer-events 透传）帮助聚焦。
- **响应式**：<768px 宽时改为底部抽屉（`y: 110% → 0` 上滑，圆角顶边 20px）。
- **视觉**：沿用现有卡通圆角玻璃卡（白 62% + blur 14 + 蔷薇描边 + 柔阴影）；顶部成分色条（与被点球同色）。

### 验收

- 点击 VC/VE/阿魏酸/烟酰胺：卡片从右侧弹出且内容正确；切换内容淡切无位移；三种关闭方式都有效；1440×900 与 390×844（移动端底部抽屉）截图确认无重叠、无抖动。

---

## 改动二：入场运镜动画（Opening Sequence）

### 目标体验

打开网页 → 加载幕 → 一个**桌面场景**的大远景：桌上立着这瓶化妆品，桌后是唯美动漫风背景 → 镜头由远及近缓推，聚焦到这瓶上 → slogan/UI 渐次浮现 → 进入可交互状态。

### 时间轴（总长约 4.5s，可跳过）

| 阶段 | 时间 | 内容 |
|---|---|---|
| P0 加载幕 | 0–0.6s | 贝母白全屏 + 中央 logo「成分真言」呼吸 + 底部进度 shimmer（随真实资源加载进度：字体/首帧渲染完成才放行） |
| P1 运镜 | 0.6–4.0s | 相机由大远景 `(0, 5.5, 17)`（桌面全景 + 背景可见）缓推至 Hero 机位 `(0, 1.7, 7.2)`，ease-in-out（cubic-bezier(0.65, 0, 0.35, 1)）；前 40% 以背景/桌面为主体，后 60% 聚焦瓶子；可选轻微焦点过渡（焦点距离 18 → 7.5，DOF 散景同步收拢） |
| P2 UI 入场 | 4.0–4.6s | eyebrow → 主标题 → 副文 → CTA 依次 fade-up（各延 80ms，y 14→0, opacity 0→1）；成分球在液体内淡入浮现；图例最后淡入 |
| P3 就绪 | 4.6s– | 全部交互开放 |

- **跳过**：P1 期间点击/按键立即快进到 P2 末尾态（300ms 过渡）。
- **无障碍**：`prefers-reduced-motion` 用户直接终态，无运镜。
- **重复访问**：同一标签页会话只播一次（sessionStorage 标记），刷新重播。

### 场景新增物

- **桌面**：浅暖木纹长桌（程序化 canvas 纹理：贝母暖白底 + 淡木纹丝 + 微反光 roughness≈0.5），圆角厚板，前缘出画；瓶子立于桌面（取消大幅悬浮，仅保留 ±1.5% 呼吸浮动），ContactShadows 落桌面；桌面后缘自然衔接背景。
- **背景（动漫唯美风，全部程序化生成、零外部图片）**：
  - 天空：垂直渐变（贝母白 #f3ede6 → 淡粉 #f6d9e2 → 薰衣草 #dccdf0），着色器或大平面渐变；
  - 云：3–4 朵卡通风圆胖云（多个白色 toon 球体拼合 + 柔描边），不同深度、不同速度缓慢漂移（循环）；
  - 光斑：远景散景圆斑（淡色透明圆片，缓慢浮动）营造空气感；
  - 花瓣/星粒：少量粉色花瓣（心形小片）与星形微粒从上方缓缓飘落、触底消失循环；
  - 地面远景：桌面尽头下接一层淡色雾化，避免「悬空桌」。
- **运镜期构图**：远景时桌面占下 1/3、背景占上 2/3；推近后落到现有 Hero 构图（左 slogan 右瓶）。

### 技术要点

- 相机动画：自写 lerp/ease 驱动 `camera.position` 与 `controls.target`（或 drei `CameraControls` 的 `setLookAt` 带 smoothTime）；禁用用户缩放（运镜期），运镜结束后恢复仅视差跟随。
- DOF（可选增强）：postprocessing DepthOfField，focalLength 随运镜从 0.02 收到 0.008；若 SwiftShader 截图困难则仅真机启用。
- 性能：花瓣/星粒用 InstancedMesh；dpr ≤1.75；首帧加载完才进 P1（loading 与 drei `useProgress` 联动）。
- 降级：WebGL 失败 → 静态贝母瓶图 + 无运镜直接显示。

### 验收

- Playwright 录屏/连拍（0s / 1.5s / 3s / 4.5s 四帧）确认运镜轨迹与构图变化；
- 终帧与现有 Hero 构图一致；P2 各元素 stagger 顺序正确；
- 跳过功能有效；`prefers-reduced-motion` 直接终态；
- 无资产外网依赖（断网可跑）；1440×900 下无掉帧感（真机目测）。

---

## 实施方式

1. 在 lab/hero-demo 原型中实现上述两项，截图/录屏验证（沿用既有子代理审查流程：实现 → 独立审查）；
2. 你确认后，连同已定的首页设计一起写进正式设计文档，再经 writing-plans 拆任务回主前端（frontend/）落地。

## 明确不做（本增量范围外）

- 页面结构（产品库/详情/成分/对比各页）——首页两项定稿后单独讨论；
- 卡片与被点球之间的引导光线（V1 省略，避免视觉噪声）；
- 移动端深度适配（仅做卡片抽屉兜底）。
