# 成分真言 · 实施计划 02：浓度区间推断引擎

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** 给「有官方降序成分表」的产品装上浓度推断能力：约束采样输出每个成分的浓度区间 + 置信度，用品牌披露浓度做锚点校准（区间覆盖率 ≥80%），再做「剂量达标判定」（推断区间 vs 文献起效浓度），经 API 与前端视图交付。

**Architecture:** 成分表 → 约束满足问题（降序 + 0.1% 微量线 + 法定/文献上限 + Σ=100 + 剂型水相先验）→ 排序化 Dirichlet 拒绝采样 → 每成分 [p5,p95] 区间 → 披露锚点回归校准 → 与 EfficacyAssertion 的文献起效浓度比对出判定（达标/存疑/不足/无法判定）。推断结果持久化到 `product_ingredients.conc_low/high/confidence`，API 只读。

**Tech Stack:** Python 3.11 · NumPy · SQLAlchemy · FastAPI · pytest · 既有前端（React+Vite）

## 全局约束（每个任务都隐含遵守）

- 项目根 `/root/workspace/olaiya`；统一 `.venv/bin/python -m pytest`（仓库根目录执行）。
- TDD：先写失败测试再实现；每个任务结束 git commit，信息格式 `type: 中文描述`（type ∈ feat/test/chore/fix）。
- **推断浓度是估计值**：所有输出字段名、API 文档字符串、前端展示必须带「估计」语义，前端必须显著标注。
- **没有官方降序成分表的产品绝不推断**（position 为 NULL 的产品直接跳过并说明原因），不伪造位次。
- 披露锚点（品牌公布浓度）只用于校准与验收，不得回写为推断值。
- 采样必须可复现：所有随机过程显式传 seed。

## 数据前提（当前真实状态）

- 采集产品（盖德镜像）成分为**拼音序**，`position=NULL` —— 不可推断。
- 可推断产品 = 种子 2 个（修丽可 CE、The Ordinary 烟酰胺，公开 INCI 降序 + 品牌披露浓度）+ 本计划 T1 新增的 6-8 个官方降序成分表产品。
- 文献起效浓度来自 `efficacy_assertions.effective_conc_low/high`（已有 62 条断言，约 20 条带浓度）。

---

### Task 1: 官方降序成分表产品数据集（含披露锚点）

**Files:**
- Create: `/root/workspace/olaiya/data/seed/ordered_products.json`
- Modify: `/root/workspace/olaiya/data/loaders/seed_loader.py`（支持该产品集与 disclosed 锚点）
- Modify: `/root/workspace/olaiya/backend/app/models/product.py`（ProductIngredient 加 `disclosed_conc` 列）
- Test: `/root/workspace/olaiya/backend/tests/test_seed_loader.py`（追加）

**内容要求：**
- 6-8 个知名产品，每个含：`name / brand / category / price_current（人工采样可空）/ note（来源标注：公开包装 INCI 降序，可能与备案版略有差异）/ ingredients（INCI 英文大写降序数组）/ trace_ingredients / disclosed（{INCI名: 披露浓度%}，无披露则空对象）/ ingredient_source_url`。
- 候选产品（实现时用 WebSearch/FetchURL 到 INCIDecoder、品牌官网等核实成分表与顺序，来源 URL 必须真实记入；核不准顺序的产品宁可不收录）：
  1. 兰蔻小黑瓶精华（Advanced Génifique）
  2. 雅诗兰黛小棕瓶（Advanced Night Repair）
  3. SK-II 神仙水（Facial Treatment Essence）
  4. OLAY 淡斑小白瓶（Pro-X 亮洁晳颜祛斑精华）
  5. 理肤泉 B5 多效修复霜（Cicaplast Baume B5，品牌公布 5% 泛醇）
  6. 薇诺娜舒敏保湿特护霜
  7. 赫莲娜黑绷带面霜（活颜修护舒缓晚霜，品牌公布 30% 玻色因）
  8. 修丽可紫米精华（H.A. Intensifier，品牌公布 10% 玻色因 + 2% 甘草酸二钾 + 0.2% 紫米）
- `disclosed` 锚点至少覆盖 8 个成分浓度点（含已有种子 2 产品的 5 个点）。
- 加载：写入 `products` + `product_ingredients`（position 1-based 真实降序、is_trace），disclosed 写入新列 `disclosed_conc`；幂等；这些产品带 ingredient_source_url 存进 product.source_url。
- 测试：加载后产品数、位次严格递增、锚点数量、幂等；`test_db` 无需改。

**Interfaces:**
- Produces: `ProductIngredient.disclosed_conc: float|None`；`data/seed/ordered_products.json`；加载器函数 `load_ordered_products(session, path=ORDERED_PATH) -> None`（seed_loader 的 main 调用它）。

---

### Task 2: 推断引擎核心（约束采样）

**Files:**
- Create: `/root/workspace/olaiya/backend/app/services/__init__.py`（空）
- Create: `/root/workspace/olaiya/backend/app/services/concentration.py`
- Test: `/root/workspace/olaiya/backend/tests/test_concentration.py`

**接口（Produces，后续任务依赖的精确签名）：**

```python
@dataclass
class IngredientInput:
    inci_name: str
    is_trace: bool = False
    upper_cap: float | None = None   # min(legal_cap, iecic_max, cir_high)，无则 None
    water: bool = False              # 是否水（水相先验的对象）

@dataclass
class ConcentrationEstimate:
    inci_name: str
    low: float          # p5
    high: float         # p95
    confidence: float   # 0-1

def estimate_concentrations(
    ingredients: list[IngredientInput],
    *,
    leave_on: bool = True,       # 驻留类(True)/淋洗类(False)，影响水相先验
    n_samples: int = 2000,
    seed: int = 42,
) -> list[ConcentrationEstimate]
```

**算法（排序化 Dirichlet 拒绝采样）：**
1. 主成分段（is_trace=False，共 m 个）：从 Dirichlet(1,...,1) 采总和为 S_main 的 m 个值并降序排列，S_main = 100 − S_trace。
2. 微量段（is_trace=True，共 t 个）：每个从 U(0, min(0.1, cap)) 采样，总和 S_trace 由主段剩余决定；归一化到剩余量并拒绝任何 >0.1 的样本。
3. 约束拒绝：主段末位必须 ≥0.1（微量线定义）；任何成分不得超过 upper_cap；leave_on 且 water=True 的成分必须在 [50, 95]；淋洗类水相先验 [40, 90]。
4. 拒绝采样最多 n_samples×20 次，收集 n_samples 个有效样本；不足则报错（说明约束矛盾，不得静默放宽）。
5. 输出：每成分取样本的 p5/p95 为 low/high；confidence = 0.5 + 0.3×(有 cap 或 water 先验) + 0.2×(1 − 归一化区间宽度)，截断到 [0,1]；同一批输入同一 seed 输出必须逐位一致（可复现测试）。
6. upper_cap 的组装不在本函数内（调用方从 Ingredient priors 取 min）。

**测试要点：** 约束恒成立（乱序断言全样本满足降序/微量线/Σ≈100/cap）；可复现性；水相先验；微量段 ≤0.1；典型产品（修丽可 CE 12 成分）区间合理性（水在首位且区间最大）；约束矛盾时显式报错。

---

### Task 3: 锚点校准与推断执行器

**Files:**
- Create: `/root/workspace/olaiya/data/tools/run_inference.py`
- Test: `/root/workspace/olaiya/backend/tests/test_run_inference.py`

**内容：**
1. `run_inference(session, *, n_samples=2000, seed=42) -> dict`：查所有「position 非空」的产品 → 组装 IngredientInput（cap = min(legal_cap, iecic_max_leave_on/ rinse_off 按 leave_on, cir_high)，water 判 inci_name in {"WATER", "AQUA", "水"}；leave_on 按 category 不含「洗/洁面/洗发/沐浴/卸妆」推断）→ 调引擎 → 回写 `conc_low/high/confidence` → 返回统计。
2. CLI：`PYTHONPATH=backend .venv/bin/python -m data.tools.run_inference`，打印每产品一行与锚点校准汇总。
3. **锚点校准报告**：对每个 `disclosed_conc` 非空的成分关联，判定披露值是否落在 [low, high]（允许 ±20% 相对容差），打印 `锚点覆盖率 = 命中数/总锚点数`。**验收：≥80%**；不足时调引擎先验（如 Dirichlet α 按位次衰减）再测，记录最终参数与覆盖率到报告。校准只调采样先验参数，不得动披露值或区间输出语义。
4. 测试：构造 3 个成分的玩具产品（水/烟酰胺/苯氧乙醇+微量段），跑 run_inference 断言字段回写、cap 生效（苯氧乙醇 high ≤1.0）、幂等（重跑结果一致）。

---

### Task 4: 剂量达标判定 + API

**Files:**
- Create: `/root/workspace/olaiya/backend/app/services/dosecheck.py`
- Modify: `/root/workspace/olaiya/backend/app/main.py`
- Test: `/root/workspace/olaiya/backend/tests/test_api.py`（追加）

**判定逻辑（精确语义，Produces `verdict_for(low, high, eff_low, eff_high) -> str`）：**
- 无断言或 eff_low 为 None → `"unknown"`（无法判定：缺文献浓度）
- `low >= eff_low` → `"effective"`（达标：区间整体过起效线）
- `high < eff_low` → `"insufficient"`（不足：区间整体低于起效线）
- 其他 → `"uncertain"`（存疑：区间横跨起效线）
- 以上判定在产品无推断结果（conc_low 为 NULL）时不输出判定字段。

**API：**
- `GET /api/products/{id}/concentration` → `{product_id, inferred: true, estimates: [{ingredient_id, inci_name, cn_name, low, high, confidence, disclosed_conc, dose: {efficacy, eff_low, eff_high, verdict}[]} ]}`；产品无位次成分表时返回 `{product_id, inferred: false, reason: "无官方降序成分表，未推断"}`（200 而非 404）。
- 返回 JSON 的 docstring 与字段注释必须含「估计」语义。

**测试：** 有推断产品返回估计与判定结构；无位次产品 inferred=false；判定逻辑四类分支单测（effective/insufficient/uncertain/unknown）。

---

### Task 5: 前端剂量判定视图

**Files:**
- Modify: `/root/workspace/olaiya/frontend/src/pages/ProductDetail.jsx`（或新建 ConcentrationPanel 组件）
- Modify: `/root/workspace/olaiya/frontend/src/api.js`

**内容：**
- 产品详情页新增「浓度推断（估计值）」区块：每个成分的区间条形展示（low–high 横条），披露锚点以竖线标记；剂量判定徽章（达标=绿 / 存疑=黄 / 不足=红 / 无法判定=灰）挂在有断言的成分上。
- 区块顶部固定提示：「以下浓度为模型估计值，非官方数据」；无位次产品显示「该产品暂无官方降序成分表，未做浓度推断」。
- 构建（cd frontend && npm run build）并用 Playwright 截图验证（修丽可 CE 页应见 15%VC 锚点线与判定徽章）。

---

## 完成定义（Definition of Done）

- [ ] `.venv/bin/python -m pytest` 全绿（新增 ≥12 个用例）
- [ ] CLI 推断执行器输出锚点覆盖率 ≥80%
- [ ] API：修丽可 CE 返回区间+判定，盖德采集产品返回 inferred=false
- [ ] 前端剂量区块截图确认（含「估计值」标注与锚点线）
- [ ] git log 每任务一个提交，工作区干净
