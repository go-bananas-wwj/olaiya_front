# data/models —— 模型权重目录

统一存放本项目所有模型权重与训练产物（大文件，不进 git）：

- `embedding/`：句向量模型（BGE-M3，约 2GB，唯一向量通道）
- `llm/`：对话大模型（如 Qwen2.5-14B，约 28GB）
- ~~`ingredient2vec/`：自训练成分向量~~（2026-07-28 决策：整体取消——67 产品语料训 word2vec 无统计意义，见创新计划 v3 §四）

下载/训练脚本统一从本目录读写；权重路径通过环境变量或配置文件注入，不硬编码进代码。
