# 设计增量计划 #2：瓶内圆环去除 + 背景墙改「开着的窗户」

> 状态：待用户批准 ｜ 日期：2026-07-28 ｜ 范围：首页 Hero 场景（lab/hero-demo）
> 已确认体验不变的部分：珍珠贝母配色、童话卡通风、入场运镜结构（远景→聚焦）、成分卡右弹、成分球交互

---

## 改动一：瓶内漩涡环带去除

- **现状问题**：液体内有一圈漩涡环带（读感像「土星环」），用户要求透明化。
- **执行**：整体移除环带 mesh（不是调透明度，直接删除）；液体保持干净的粉色卡通渐变。
- **补偿（防单调）**：仅保留既有的缓慢上浮小气泡（数量 ≤6、尺寸小、低存在感的半透白点），不再添加任何环状/带状装饰。
- **验收**：终帧与近景截图中瓶内无环带；液体读感为干净粉冻 + 成分球主角。

## 改动二：液面顶部白环修复（新增）

- **现状问题**：液体顶面渲染成一个硬白色圆环/椭圆（应为液面，视觉上是 bug）。
- **执行**：重做为与液体同色系的液面——顶面椭圆用液体浅色端粉色（比液柱浅 10-15%）+ 软边过渡；正面视角下液面厚度（椭圆高度）克制（≤瓶内径 6%）；允许在液面中心加一处极淡的高光点（模拟窗光反射，透明度 ≤35%），禁止任何硬边白环/白色描边。
- **验收**：近景截图液面与液柱颜色连续自然，无白色环带；侧面轮廓无白边。

## 改动二：背景墙 →「开着的窗户」场景

### 场景构成（全部程序化生成，零外部图片）

1. **墙面**：暖贝母灰泥墙（比贝母白略深一号 #e8ddd2 附近，带轻微颗粒/暗角），让窗户成为视觉光源中心；桌面保持不变（浅暖木纹）。
2. **窗户本体**：
   - 位置：画面中右部、瓶子正后方偏右，使瓶身获得逆光轮廓；
   - 结构：暖木质窗框（与桌面同色系深一号），四格窗棂；**一侧窗扇向内打开约 30-40°**（开着的窗户），窗扇玻璃带轻微透明反光；
   - 尺寸：约占背景宽度的 1/4-1/3，远景构图时是画面第二视觉中心（第一是瓶子）。
3. **窗外世界**（透过窗格可见）：
   - 粉紫动漫天空渐变（沿用现有配色：贝母白→淡粉→薰衣草）；
   - 2-3 朵卡通圆胖云缓慢漂移（循环，方向一致）；
   - 远景淡色散景光斑（空气感）；
   - 左下角一枝樱花树枝剪影探入窗框一角（深蔷薇色卡通剪影，带几朵花）。
4. **光线设计**（本改动的灵魂）：
   - 主光改从窗户方向来：瓶子获得柔和的背光/轮廓光（蔷薇暖色 rim light）；
   - **窗格光路**：2-3 束柔和光带透过窗棂斜落到桌面上（低透明渐变光锥，缓慢呼吸微动），桌面出现对应的窗格亮斑；
   - 光路中悬浮细小光尘（微尘粒子，仅存在于光锥内，缓慢上升）。
5. **花瓣动线**：樱花花瓣从开着的窗扇外飘入室内，掠过瓶侧，落到桌面上消失（替代现有「从天而降」的花瓣）；数量克制（平均每秒 ≤1 片）。
6. **云与星心粒子**：室内漂浮的小星星/小爱心保留但数量减半（让位于窗户叙事）。

### 与运镜的衔接

- P1 大远景构图：桌面下 1/3 + 墙面窗户中右 + 窗外天空——窗户是远景阶段的视觉锚点；推近终点构图不变（瓶子主体 + 窗口在背景柔化存在）。
- 相机动画参数、时间轴、跳过逻辑、reduced-motion 行为全部不变。

### 验收

- 连拍四帧（0/1.5/3/4.5s）：远景帧能清晰读出「开着的窗户 + 窗外天空」；终帧瓶后有柔光轮廓；
- 瓶内无环带；花瓣从窗扇飘入的动线可见（连拍至少 1 帧花瓣在窗与瓶之间）；
- 交互路径（点成分球→右弹卡、跳过运镜）回归通过；无外部资产请求；
- Playwright 截图至少 3 轮迭代自查。

## 明确不做

- 不做窗外场景的深度交互（如点击窗户）；
- 不做雨天/夜晚等其他窗景变体（若你喜欢这版，变体另议）；
- 不改 slogan 区、成分卡、轮播概念（多瓶轮播仍在设计文档阶段，本增量不动）。

## 实施与提交

lab/hero-demo 内实现 → 截图验证 → `git commit -m "lab: 去除瓶内环带，背景墙改开着的窗户场景"` → 我审图后向你展示，等你修改意见。
