# 颜鉴一期页面改版 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按《成分真言-设计方案-页面功能-v2.md》（下称 v2.2 方案）上线一期 9 页：首页/产品库/产品详情/成分库/成分详情/排行榜/对比/解码/AI 问答，更名「颜鉴」，圆桌与鉴伪摘除入口。

**Architecture:** 后端仅加 4 个最小查询参数（产品 efficacy/sort/q+备案号、成分 q 折叠匹配），其余全部前端；前端沿用 React+Vite+Tailwind+HashRouter（`frontend/src/` → 构建到 `web/`），童话风令牌已定稿。线框参考图在 `http://127.0.0.1:8008/wireframes/`（源码 `lab/wireframes/index.html`）。

**Tech Stack:** FastAPI+SQLAlchemy+SQLite（后端）；React 18+Vite 5+Tailwind 3+react-router HashRouter（前端）；pytest（后端 TDD）；Playwright（/tmp/pwenv/bin/python，前端截图验收）。

## Global Constraints

- 数据铁律：估计值必带「估计」语义；查不到如实说不编造；弱证据 note 照录；文献链接跳真实出处；无数据项不排不猜
- 文案：少字说人话；正文 Quicksand+Noto Sans SC（`font-pearl`）、标题/导航/按钮站酷快乐体（`font-display`）、数字 Baloo 2（`font-num`）；配色 pearl #f3ede6 / pearl-ink #3d2f2a / rosewood #b06a8a / iris #8a7ab8 / mint #6fae9e（`frontend/tailwind.config.js` 与 `frontend/src/index.css` 已定义，直接用不新造）
- 品牌名一律「颜鉴」，slogan「美，经得起逐滴核验」
- 后端测试：仓库根目录 `.venv/bin/python -m pytest`；TDD：先写失败测试再实现
- 后端测试 fixture 模式：参照 `backend/tests/test_pagination.py`（`client` fixture + `load_seed` 或自建行 + `TestClient`）
- 前端验证：`cd frontend && npm run build` 通过 + Playwright 截图自查（chromium 参数 `--no-sandbox --disable-dev-shm-usage`，8008 端口）
- 服务：tmux 会话 `cfz-web`（8008）；改后端后须重启：`tmux kill-session -t cfz-web && tmux new-session -d -s cfz-web -c /root/workspace/olaiya "PYTHONPATH=backend .venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8008"`
- 提交：`type: 中文描述`（type ∈ feat/fix/test/chore），**每个任务一个提交，提交后立即 `git push origin master`**；只 `git add` 本任务相关文件（工作区有并行会话的无关改动，严禁混入）
- 圆桌 `/roundtable`、鉴伪 `/detect`：路由与文件保留，摘除所有入口链接，页顶加「实验功能」灰标

---

### Task 1: 后端 /api/products 三参数（efficacy / sort / q 并备案号）

**Files:**
- Modify: `backend/app/main.py:196-244`（`list_products`）
- Test: `backend/tests/test_products_filters.py`（新建）

**Interfaces:**
- Produces: `GET /api/products?efficacy=美白&sort=claim_count_desc&q=…`——efficacy 六枚举（美白/抗老/保湿/祛痘/舒缓/防晒），未知值 422；sort 二枚举（claim_count_desc/ingredient_count_desc），未知值 422，缺省保持 `order_by(Product.brand, Product.id)`；q 额外匹配 `Product.nmpa_id`；返回结构不变（纯 list 或 `{total, items}` 双形态）
- Consumes: 无（首批任务）

- [ ] **Step 1: 写失败测试**

新建 `backend/tests/test_products_filters.py`：

```python
"""产品列表新参数：efficacy 功效胶囊筛选 / sort 排序 / q 并入备案号匹配（v2.2 方案 P0）。"""

import pytest
from fastapi.testclient import TestClient

from app.main import app, get_db
from app.models.product import Product, ProductClaim


@pytest.fixture()
def client(session):
    p1 = Product(name="甲 美白精华", brand="甲牌", nmpa_id="浙G妆网备字2026000001")
    p2 = Product(name="乙 保湿面霜", brand="乙牌", nmpa_id="沪G妆网备字2026000002")
    p3 = Product(name="丙 无宣称乳", brand="丙牌", nmpa_id=None)
    session.add_all([p1, p2, p3])
    session.flush()
    session.add_all([
        ProductClaim(product_id=p1.id, claim="美白提亮"),
        ProductClaim(product_id=p1.id, claim="保湿"),
        ProductClaim(product_id=p2.id, claim="保湿"),
    ])
    session.commit()
    app.dependency_overrides[get_db] = lambda: session
    try:
        with TestClient(app) as c:
            yield c
    finally:
        app.dependency_overrides.clear()


def test_efficacy_filter_hit():
    r = client.get("/api/products", params={"efficacy": "美白"}).json()
    assert [p["name"] for p in r] == ["甲 美白精华"]


def test_efficacy_filter_keyword_or():
    # 「美白」枚举映射多词 OR：claim「美白提亮」命中；「保湿」枚举不命中它
    r = client.get("/api/products", params={"efficacy": "保湿"}).json()
    names = sorted(p["name"] for p in r)
    assert names == ["乙 保湿面霜", "甲 美白精华"]


def test_efficacy_unknown_422():
    assert client.get("/api/products", params={"efficacy": "丰胸"}).status_code == 422


def test_sort_claim_count_desc():
    r = client.get("/api/products", params={"sort": "claim_count_desc"}).json()
    assert [p["name"] for p in r][:2] == ["甲 美白精华", "乙 保湿面霜"]


def test_sort_unknown_422():
    assert client.get("/api/products", params={"sort": "price"}).status_code == 422


def test_q_matches_nmpa_id():
    r = client.get("/api/products", params={"q": "2026000002"}).json()
    assert [p["name"] for p in r] == ["乙 保湿面霜"]


def test_no_params_backward_compatible():
    r = client.get("/api/products").json()
    assert isinstance(r, list) and len(r) == 3
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest backend/tests/test_products_filters.py -q`
Expected: FAIL（efficacy/sort 参数未实现时过滤不生效或 422 缺失）

- [ ] **Step 3: 实现（backend/app/main.py）**

在文件顶部常量区（`app = FastAPI(...)` 之前）加：

```python
# 功效胶囊筛选枚举 → 宣称关键词（多词 OR；拿不准的宣称不归类，同铁律 7 口径）
EFFICACY_KEYWORDS = {
    "美白": ["美白", "淡斑", "祛斑", "提亮"],
    "抗老": ["抗皱", "紧致", "淡纹", "抗老", "抗衰"],
    "保湿": ["保湿", "滋润", "补水"],
    "祛痘": ["祛痘", "清痘", "抗痘", "净痘"],
    "舒缓": ["舒缓", "舒敏", "修护"],
    "防晒": ["防晒"],
}
PRODUCT_SORTS = {"claim_count_desc", "ingredient_count_desc"}
```

`list_products` 签名加参数并加逻辑（接在 has_claims 过滤之后、`_item` 定义之前）：

```python
def list_products(q: str | None = None, brand: str | None = None,
                  has_claims: str | None = None, efficacy: str | None = None,
                  sort: str | None = None,
                  limit: int = Query(0, ge=0), offset: int = Query(0, ge=0),
                  db: Session = Depends(get_db)):
```

```python
    if q:
        like = f"%{q}%"
        stmt = stmt.where(or_(Product.name.like(like), Product.brand.like(like),
                              Product.nmpa_id.like(like)))
    if efficacy is not None:
        kws = EFFICACY_KEYWORDS.get(efficacy)
        if kws is None:
            raise HTTPException(status_code=422, detail=f"未知功效枚举: {efficacy}")
        stmt = stmt.where(Product.id.in_(
            select(ProductClaim.product_id)
            .where(or_(*[ProductClaim.claim.like(f"%{k}%") for k in kws]))))
    if sort is not None:
        if sort not in PRODUCT_SORTS:
            raise HTTPException(status_code=422, detail=f"未知排序: {sort}")
        stmt = stmt.order_by(
            claim_cnt.desc() if sort == "claim_count_desc" else ing_cnt.desc(),
            Product.id)
    else:
        stmt = stmt.order_by(Product.brand, Product.id)
```

（原 `stmt = stmt.order_by(Product.brand, Product.id)` 一行删除，由上面 else 分支接管。）

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `.venv/bin/python -m pytest backend/tests/test_products_filters.py -q` → PASS
Run: `.venv/bin/python -m pytest -q` → 全绿

- [ ] **Step 5: 重启服务 + 真实数据抽查 + 提交**

```bash
tmux kill-session -t cfz-web; tmux new-session -d -s cfz-web -c /root/workspace/olaiya "PYTHONPATH=backend .venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8008"
sleep 5
curl -s "http://127.0.0.1:8008/api/products?efficacy=美白&limit=3" | head -c 400
git add backend/app/main.py backend/tests/test_products_filters.py
git commit -m "feat: 产品列表新增 efficacy/sort 参数与备案号搜索"
git push origin master
```

---

### Task 2: 后端 /api/ingredients q 折叠匹配（解码页依赖）

**Files:**
- Modify: `backend/app/main.py:84-123`（`list_ingredients`）
- Test: `backend/tests/test_ingredients_fold.py`（新建）

**Interfaces:**
- Produces: `GET /api/ingredients?q=…`——q 对 inci_name 做折叠匹配（忽略大小写/空格/连字符），对 cn_name 保持原样 LIKE；返回结构不变
- Consumes: 无

- [ ] **Step 1: 写失败测试**

```python
"""成分搜索折叠匹配：忽略大小写/空格/连字符（解码页逐成分查询依赖，v2.2 方案 P0）。"""

import pytest
from fastapi.testclient import TestClient

from app.main import app, get_db
from app.models.ingredient import Ingredient


@pytest.fixture()
def client(session):
    session.add_all([
        Ingredient(inci_name="NIACINAMIDE", cn_name="烟酰胺"),
        Ingredient(inci_name="HYALURONIC ACID", cn_name="透明质酸"),
    ])
    session.commit()
    app.dependency_overrides[get_db] = lambda: session
    try:
        with TestClient(app) as c:
            yield c
    finally:
        app.dependency_overrides.clear()


def test_fold_case_space():
    r = client.get("/api/ingredients", params={"q": "niacin amide"}).json()
    assert [i["cn_name"] for i in r] == ["烟酰胺"]


def test_fold_hyphen():
    r = client.get("/api/ingredients", params={"q": "hyaluronic-acid"}).json()
    assert [i["cn_name"] for i in r] == ["透明质酸"]


def test_cn_name_unchanged():
    r = client.get("/api/ingredients", params={"q": "烟酰"}).json()
    assert [i["cn_name"] for i in r] == ["烟酰胺"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest backend/tests/test_ingredients_fold.py -q`
Expected: `test_fold_case_space`、`test_fold_hyphen` FAIL

- [ ] **Step 3: 实现**

`list_ingredients` 的 q 分支改为：

```python
import re  # 文件顶部若无则加

_FOLD_RE = re.compile(r"[\s\-]+")

    if q:
        like = f"%{q}%"
        folded = _FOLD_RE.sub("", q).lower()
        stmt = stmt.where(or_(
            Ingredient.cn_name.like(like),
            Ingredient.inci_name.like(like),
            func.replace(func.replace(func.lower(Ingredient.inci_name), " ", ""), "-", "")
                .like(f"%{folded}%"),
        ))
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `.venv/bin/python -m pytest backend/tests/test_ingredients_fold.py backend/tests/test_pagination.py -q` → PASS
Run: `.venv/bin/python -m pytest -q` → 全绿

- [ ] **Step 5: 重启服务 + 提交**

```bash
tmux kill-session -t cfz-web; tmux new-session -d -s cfz-web -c /root/workspace/olaiya "PYTHONPATH=backend .venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8008"
sleep 5
curl -s "http://127.0.0.1:8008/api/ingredients?q=niacin%20amide&limit=2"
git add backend/app/main.py backend/tests/test_ingredients_fold.py
git commit -m "feat: 成分搜索支持折叠匹配（忽略大小写/空格/连字符）"
git push origin master
```

---

### Task 3: 全局外壳——更名颜鉴 + 顶栏 6 项 + 摘除圆桌/鉴伪入口 + 全局搜索 + 统一搜索页

**Files:**
- Modify: `frontend/src/components/Layout.jsx`（顶栏/页脚）
- Modify: `frontend/src/App.jsx`（路由：加 `/search`；圆桌/鉴伪保留路由）
- Modify: `frontend/src/pages/Roundtable.jsx`、`frontend/src/pages/Detect.jsx`（页顶加「实验功能」灰标）
- Create: `frontend/src/pages/SearchResults.jsx`
- Modify: `frontend/src/api.js`（加 search 组合调用）
- 全站改名点：Layout 标题、首页 Home.jsx、`frontend/index.html` title、页脚

**Interfaces:**
- Consumes: Task 1/2 的后端参数（本任务只用现有 q）
- Produces: 顶栏最终 6 项导航（产品库/成分库/排行榜/对比/解码/AI 问答）+ 全局搜索框（回车跳 `#/search?q=…`）；`api.searchAll(q)` → `{products, ingredients}`（并行 `api.products({q, limit:8})` + `api.ingredients({q, limit:8})`，取 items 或纯 list 两形态兼容）；`SearchResults` 路由页

- [ ] **Step 1: Layout 换导航与改名**

NAV 数组改为（顺序固定）：

```js
const NAV = [
  { to: '/products', label: '产品库' },
  { to: '/ingredients', label: '成分库' },
  { to: '/rankings', label: '排行榜' },
  { to: '/compare', label: '对比' },
  { to: '/decode', label: '解码' },
  { to: '/chat', label: 'AI 问答' },
]
```

logo 文案「成分真言」→「颜鉴」；副标题改为 slogan「美，经得起逐滴核验」。顶栏右侧加搜索框（受控 input，`onKeyDown` 回车 `navigate(`/search?q=${encodeURIComponent(kw)}`)`），placeholder「搜产品 / 成分 / 备案号…」。页脚「成分真言 · 欧莱雅美妆科技黑客松 2026」→「颜鉴 · 欧莱雅美妆科技黑客松 2026」。

- [ ] **Step 2: SearchResults 页**

`frontend/src/pages/SearchResults.jsx`：读 `useSearchParams` 的 q；并行调两接口（`useFetch` 参照现有用法）；分两段展示——产品段复用 `ProductCard`，成分段复用成分卡片样式（中文名/INCI/断言数，链到详情）；两段标题显示命中数；无结果段各自显示「没找到相关产品，试试解码成分表 →」「没找到相关成分」；q 变化时重新请求（useFetch 依赖含 q）。

`api.js` 加：

```js
searchAll: async (q) => {
  const [p, i] = await Promise.all([
    api.products({ q, limit: 8 }),
    api.ingredients({ q, limit: 8 }),
  ])
  return { products: p.items ?? p, ingredients: i.items ?? i }
},
```

- [ ] **Step 3: 圆桌/鉴伪加灰标 + 路由登记 /search**

Roundtable.jsx 与 Detect.jsx 页顶加一行：`<div className="pearl-badge-muted mb-3">实验功能 · 二期上线</div>`。
App.jsx 加 `<Route path="search" element={<SearchResults />} />`；`/rankings`、`/decode` 路由本任务先指向占位组件（创建最小 `Rankings.jsx`/`Decode.jsx` 返回 `<div className="pearl-page"><div className="glass-card relative p-8 text-center text-pearl-ink-2">建设中 · 排行榜/解码即将上线</div></div>`），后续任务替换。

- [ ] **Step 4: 全站改名扫描**

Run: `grep -rn "成分真言" frontend/src frontend/index.html`——除页脚「数据链路」等历史文案外全部替换为「颜鉴」；线框图与文档里的旧名不动。

- [ ] **Step 5: 构建 + 截图验证 + 提交**

```bash
cd frontend && npm run build && cd ..
/tmp/pwenv/bin/python - <<'EOF'
from playwright.sync_api import sync_playwright
with sync_playwright() as p:
    b = p.chromium.launch(args=['--no-sandbox','--disable-dev-shm-usage'])
    pg = b.new_page(viewport={'width':1440,'height':900})
    pg.goto('http://127.0.0.1:8008/#/search?q=烟酰胺', wait_until='networkidle')
    pg.wait_for_timeout(2000); pg.screenshot(path='/tmp/t3_search.png')
    b.close()
print('ok')
EOF
grep -rn "roundtable\|detect" frontend/src/components/Layout.jsx   # 期望：无输出（入口已摘除）
git add frontend/src frontend/index.html web
git commit -m "feat: 更名颜鉴+顶栏六项导航+全局搜索统一结果页+摘除圆桌鉴伪入口"
git push origin master
```

---

### Task 4: 首页重写（实用门户）

**Files:**
- Modify: `frontend/src/pages/Home.jsx`（整页重写）
- Create: `frontend/src/components/EfficacyBoard.jsx`（功效榜组件，首页 Top5 与排行榜页共用）

**Interfaces:**
- Consumes: `api.products({efficacy, sort:'claim_count_desc', limit:5})`（Task 1）、`api.stats()`（现有）
- Produces: `EfficacyBoard({limit})` 组件——Tab 切换 美白/抗老/保湿/祛痘，榜单行 = 排名+产品名（Link 到详情）+一句话理由（宣称数/达标信息，无则「宣称 N 条」），行点击进详情；排行榜页 Task 6 复用

- [ ] **Step 1: EfficacyBoard 组件**

```jsx
// 关键逻辑：四个 Tab（EFFICACY_TABS = ['美白','抗老','保湿','祛痘']），
// 切 Tab 调 api.products({ efficacy: tab, sort: 'claim_count_desc', limit })
// 行：第 N 名 · 产品名 · 「宣称 N 条」；空态「该功效暂无足够数据」
// 口径注一行：「按宣称与证据数据排序，非效果排名」
```

- [ ] **Step 2: Home.jsx 重写**

布局（参照 wireframes 首页框）：
1. 首屏居中：快乐体大标题「颜鉴」+ slogan + 大搜索框（回车跳 `/search`）+ 功效胶囊四个（点击跳 `/products?efficacy=美白`，产品库读 URL 参数——Task 5 实现，本任务先带参跳转）
2. 数据规模一行（`api.stats()`）：「3749 款产品 · 4217 个成分 · 321 条真实文献证据」（数字实时取，不写死）
3. EfficacyBoard limit=5 + 「完整榜单 →」跳 `/rankings`
4. 三入口卡：查产品 / 解码成分表 / AI 问答（各一句话说明 + 跳转）
移除旧 Home 全部旧内容（深紫 hero 卡一并删）。

- [ ] **Step 3: 构建 + 截图自查 + 提交**

Playwright 截 `#/` 首屏与榜单区（图存 /tmp/t4_home.png），肉眼确认：童话风令牌、快乐体标题、榜单有真实数据。提交：`feat: 首页重写实用门户——搜索+功效榜+入口卡` 并 push。

---

### Task 5: 产品库升级（粘性工具条 + 功效胶囊 + 排序 + 竞态守卫）

**Files:**
- Modify: `frontend/src/pages/Products.jsx`（重构）
- Modify: `frontend/src/components/ProductCard.jsx`（信息齐备化）

**Interfaces:**
- Consumes: Task 1 参数（efficacy/sort）、Task 3 的 URL 参数约定（`?efficacy=` 入站）
- Produces: 工具条状态全量镜像到 URL（`useSearchParams`：q/efficacy/brand/has_claims/sort），可分享

- [ ] **Step 1: 请求竞态守卫 + 去重锁（P0 bug 修复）**

每次请求分配递增序号（`useRef`），响应落地前比对最新序号，过期丢弃；「加载更多」用 ref 锁保证同 offset 不并发：

```jsx
const reqSeq = useRef(0)
const loadingRef = useRef(false)
// 请求前：const seq = ++reqSeq.current
// 落地时：if (seq !== reqSeq.current) return
// 加载更多：if (loadingRef.current) return; loadingRef.current = true; finally 解锁
```

- [ ] **Step 2: 粘性工具条**

`sticky top-3 z-20` 玻璃条：搜索框（250ms 防抖）｜功效胶囊六枚（美白/抗老/保湿/祛痘/舒缓/防晒，单选切换）｜品牌下拉（`/api/brands`）｜「只看有宣称」开关｜排序下拉（默认/宣称多→少/成分多→少）。所有状态写 URL。筛选变化时重置 offset 重新查询。

- [ ] **Step 3: ProductCard 齐备化**

名称/品牌/备案号/宣称数/成分数 + 有无证据小徽章；空态：筛选无结果→「一键清空筛选」按钮（清 URL 参数）；全库无匹配→「试试解码成分表 →」跳 `/decode`。

- [ ] **Step 4: 构建 + 截图 + 快速连切筛选无串扰人工验证（Playwright 快速改 URL 参数截两图）+ 提交**

提交：`feat: 产品库粘性工具条——功效胶囊/排序/备案号搜索+竞态守卫` 并 push。

---

### Task 6: 排行榜页 `/rankings`

**Files:**
- Create: `frontend/src/pages/Rankings.jsx`（替换 Task 3 占位）
- Modify: `frontend/src/components/EfficacyBoard.jsx`（支持完整榜单模式 limit=20 + 锚点）

**Interfaces:**
- Consumes: `EfficacyBoard`（Task 4）；成分证据榜用 `api.ingredients({has_evidence:'true', limit:200})` 前端按 assertion_count 排序（现有字段）；核验榜/性价比榜用 `data/seed` 已有浓度推断产品子集——先调 `/api/products?has_claims=true&limit=50` 不可行处用前端聚合现有接口，**无数据项不排**
- Produces: `/rankings` 四榜：功效产品榜（Tab，主榜）/ 成分证据榜 / 产品核验榜 / 性价比榜；页顶口径横幅

- [ ] **Step 1: 页面骨架 + 口径横幅**

页顶固定：`pearl-notice`「本榜按证据强度/性价比排序，不是效果排名；无数据项不排不猜」。功效榜复用 EfficacyBoard limit=20。

- [ ] **Step 2: 成分证据榜**

`api.ingredients({has_evidence:'true', limit:200})` → 按 assertion_count 降序取 Top20，行 = 排名/中文名/INCI/断言数，链到成分详情。

- [ ] **Step 3: 核验榜 + 性价比榜**

前端聚合：对浓度推断覆盖的产品子集（先调 `/api/products?limit=200` 取候选，逐个 `/concentration` 属 N+1——改为：只在已有 `data/research/` 推断结果对应的产品 id 列表内并行请求，上限 20 个，Promise.all；**若发现无干净数据源，本榜降级为「覆盖中」占位卡，不硬排**）。达标率 = 达标数/判定总数；性价比 = 每起效成本升序。每榜注明「限有官方降序成分表的 N 款」。

- [ ] **Step 4: 首页锚点互通**

EfficacyBoard 各 Tab 加锚点 id（`board-美白` 等），首页「完整榜单 →」带锚点跳转。

- [ ] **Step 5: 构建 + 截图四榜 + 提交**

提交：`feat: 排行榜页——功效产品榜/成分证据榜/核验榜/性价比榜+口径横幅` 并 push。

---

### Task 7: 产品详情增量（功效指纹雷达条 + 匹配分 + 语义相似栏 + Highlights）

**Files:**
- Create: `frontend/src/components/FingerprintBars.jsx`（指纹雷达条）
- Create: `frontend/src/components/MatchScore.jsx`（匹配分模块 + 肤质档案设置弹层）
- Modify: `frontend/src/components/SimilarLevels.jsx`（加第四栏「语义相似」）
- Modify: `frontend/src/pages/ProductDetail.jsx`（接入三者 + Highlights 标签）
- Modify: `frontend/src/api.js`（`fingerprint(id)`、`similar(id)` 若缺则加）

**Interfaces:**
- Consumes: 现有 `/fingerprint`、`/similar`、`/concentration` 响应（读 `backend/app/main.py:308-372` 与 `backend/app/services/fingerprint.py` 确认字段）
- Produces: `MatchScore` 读取/写入 localStorage `yj_profile = {skin:'干|油|混合|敏感', goals:['美白',...]}`；产品库卡片角标（Task 5 已留位，本任务接线）

- [ ] **Step 1: FingerprintBars**

横向条形图：维=规范功效族，值=族强度（fingerprint 返回字段为准）；置顶宣称核验区上方；空态「证据不足，未生成指纹」。

- [ ] **Step 2: MatchScore**

规则（v2.2 方案）：诉求命中且有真人级证据 +20/项；命中项剂量达标 +10/项；敏感肌含酒精/香精/高浓度酸 -15/项（成分名匹配 ALCOHOL/FRAGRANCE/PARFUM/酸类清单写死组件内常量并注明口径）；每起效成本低于同诉求中位 +10；无证据支撑宣称 -10/项。UI：分数大字 + 逐条加减分行（每行可展开看依据：断言/文献名）+ 「设置我的肤质」弹层（localStorage）。数据不足（无宣称且无推断）：显示「数据不足，仅供参考」不给分。页内注明「透明匹配分：每条加减分可复核，非黑盒」。

- [ ] **Step 3: 语义相似第四栏 + Highlights**

SimilarLevels 加 Tab/栏「语义相似」调 `/api/products/{id}/similar`，注明「文本向量相似，可跨成分集合不同的产品；相似 ≠ 功效相同」。Highlights（P1）：从成分表规则推「无酒精/无香精/含防腐剂」标签，注明「由成分表规则推出」。

- [ ] **Step 4: 构建 + 截图（产品 6 与 53）+ 提交**

提交：`feat: 产品详情——功效指纹条+透明匹配分+语义相似栏` 并 push。

---

### Task 8: 成分库 + 成分详情增量

**Files:**
- Modify: `frontend/src/pages/Ingredients.jsx`（竞态守卫 + 功效族浏览 P1）
- Modify: `frontend/src/pages/IngredientDetail.jsx`（相似成分条 + 路由重置修复）
- Modify: `frontend/src/api.js`（`ingredientSimilar(id)` 若缺则加）

**Interfaces:**
- Consumes: `/api/ingredients/{id}/similar`（`backend/app/main.py:372` 读字段）
- Produces: 成分详情「相似成分」芯片条（Top5，链到对应成分页）

- [ ] **Step 1: Ingredients.jsx 竞态守卫 + 加载更多去重锁**（同 Task 5 Step 1 模式，照抄守卫代码）

- [ ] **Step 2: IngredientDetail 修复 + 相似成分条**

`useEffect(() => setExtraProducts([]), [id])`（P0 bug）；页中加「相似成分」区：调 similar 接口取 Top5 渲染芯片（fairy-chip 样式），注明「按证据文本语义相似」；空态不渲染该区。

- [ ] **Step 3: 功效族浏览（P1，可降级）**

成分库工具条加功效族下拉：选项来自 `api.ingredients({has_evidence:'true', limit:651})` 前端聚合 efficacy_canonical 不可行时（列表接口无该字段）——**降级方案：本 P1 跳过，列二期**（需后端新参数）。本任务只做 P0 两项。

- [ ] **Step 4: 构建 + 截图 + 提交**

提交：`fix: 成分库竞态守卫+成分详情路由重置修复，新增相似成分条` 并 push。

---

### Task 9: 对比页升级

**Files:**
- Modify: `frontend/src/pages/Compare.jsx`

**Interfaces:**
- Consumes: 现有双产品详情 + `/concentration` + similar-levels L1
- Produces: 招牌大数字卡 + diff 默认差异行 + 「从相似产品里挑」填充 + 可搜索 combobox

- [ ] **Step 1: 可搜索 combobox 替换双 select**

手写 combobox（输入过滤 + 键盘上下+回车选中）：输入时调 `api.products({q, limit:10})`；选中显示产品名。不引组件库。

- [ ] **Step 2: 大数字卡 + diff**

两产品都选定后：顶部大数字卡「每起效成本 ¥X/天 vs ¥Y/天（估计）」（无成本数据的产品如实标「暂无成本数据」）；成分区默认只显示差异行（独有/判定不同），「显示全部 N 种」开关。

- [ ] **Step 3: 相似填充 + 预设组**

选择器下方「不知道和谁比？→ 从相似产品里挑」：选定 A 后点击调 similar-levels L1 首个填 B 槽；预设组一组写死（The Ordinary 烟酰胺 vs 修丽可 CE，id 从库里查真实存在的填）。

- [ ] **Step 4: 构建 + 截图 + 提交**

提交：`feat: 对比页——可搜索选择器+每起效成本大数字卡+成分 diff` 并 push。

---

### Task 10: 解码页 `/decode`

**Files:**
- Create: `frontend/src/pages/Decode.jsx`（替换 Task 3 占位）

**Interfaces:**
- Consumes: `api.ingredients({q})`（Task 2 折叠匹配）
- Produces: 粘贴成分表 → 逐成分证据报告

- [ ] **Step 1: 输入解析**

文本框（placeholder 给示例前几个成分）；解析：按 `、` `，` `,` `；` 换行分割，去空白去重；「解析」按钮触发。

- [ ] **Step 2: 逐成分匹配 + 证据报告**

每个词调 `api.ingredients({q: word, limit:3})`（并发上限 4，简单队列）；命中取第一条（名称精确相等优先，其次折叠相等，否则标「存疑匹配」展示候选）；报告分两段：**已命中**（成分名+断言数+链到详情）与**未命中**（如实列「库中无此成分」）；顶部汇总行「N 个成分，命中 M 个」；注明「匹配按 IECIC 中文名/INCI 折叠比对，存疑项请人工核对」。

- [ ] **Step 3: 构建 + 截图（粘贴 OLAY 小白瓶前 10 成分实测）+ 提交**

提交：`feat: 解码成分表页——粘贴出证据报告` 并 push。

---

### Task 11: AI 问答页 polish

**Files:**
- Modify: `frontend/src/pages/Chat.jsx`

- [ ] **Step 1: 示例问题换用户原话**

欢迎区示例胶囊：「OLAY 小白瓶真的能美白吗？」「烟酰胺白天能用吗？」「有没有和修丽可 CE 差不多但更便宜的？」点击即发送。

- [ ] **Step 2: 引用 [n] 悬停证据卡**

解析回答中 `[n]` 标记，渲染为上标引用；悬停/点击弹证据卡（标题/来源/年份/链接跳原文）；hallucinated_citations 中的标号删除线红标「未核实」。读现有 Chat.jsx 与 `/api/chat` 响应结构后按现状最小改造。

- [ ] **Step 3: 构建 + 截图 + 提交**

提交：`feat: AI 问答页——用户原话示例问题+引用悬停证据卡` 并 push。

---

### Task 12: 联调验收 + 行业对标

**Files:** 无新增（只修复发现的问题，修复单独提交）

- [ ] **Step 1: 静态检查**

```bash
grep -rn "成分真言" frontend/src frontend/index.html   # 期望无输出
grep -rn "/roundtable\|/detect" frontend/src --include=*.jsx | grep -v "pages/Roundtable\|pages/Detect\|App.jsx"   # 期望无输出（零入口）
cd frontend && npm run build
.venv/bin/python -m pytest -q   # 全绿
```

- [ ] **Step 2: 九页截图走查**

Playwright 逐页截图（`#/`、`#/products`、`#/products/6`、`#/products/53`、`#/ingredients`、`#/ingredients/1`、`#/rankings`、`#/compare`、`#/decode`、`#/chat`、`#/search?q=烟酰胺`），逐张肉眼检查：童话风一致、文案少而清楚、估计语义、诚实空态。

- [ ] **Step 3: 行业对标 checklist（对照 INCIDecoder）**

逐项核对并记录差距（写进提交说明）：成分词典页要素（别名/功能标签/文献）✓？产品成分速览 ✓？解码工具 ✓？我们没有的（用户上传/评论）属主动不做。

- [ ] **Step 4: 修复走查发现的问题（每个修复独立提交）+ 最终提交**

提交：`chore: 一期九页联调验收与对标修复` 并 push。

---

## Self-Review 记录

- 规格覆盖：v2.2 方案 9 页 + 4 后端参数 + 隐藏页处理逐项有任务；功效族浏览后端参数、OCR、圆桌/鉴伪明确二期
- 排序「达标率/成本」不进产品库（空榜问题）在 Task 5 无此排序项，核验榜在 Task 6 限定子集且有降级口径
- 类型一致性：`api.searchAll`/`EfficacyBoard`/localStorage `yj_profile` 在 Task 3/4/7 间签名一致
- 每个任务独立可验（测试或截图）、独立提交

---

## 修订（2026-08-15，Task 1 落地后数据实测）

真实库 claims 是 NMPA 法定类别文本：美白/防晒宣称命中数为 0（最高频：保湿 68、修护 33、舒缓 20、紧致 19、抗皱 18、祛痘 7）。而成分证据侧 `efficacy_canonical` 覆盖厚（美白 106 断言/3670 产品、保湿 304、抗皱 117、舒缓 119、控油祛痘 54）。**功效产品榜的数据源从 claims 文本改为成分证据族**，新增任务：

### Task 2b: 后端功效榜接口 GET /api/rankings/efficacy

**Files:** Modify `backend/app/main.py`；Test: `backend/tests/test_rankings_efficacy.py`（新建）

**契约**：`GET /api/rankings/efficacy?canon=美白&limit=20`
- canon 枚举用 efficacy_canonical 真实族名：美白/抗皱/保湿/舒缓/控油祛痘/修护/抗氧化/焕肤（前端 Tab 文案可映射「抗老→抗皱、祛痘→控油祛痘」）；未知值 422
- 排名分（每产品）：该族断言的成分数 ×1 + 真人级证据断言数 ×3（evidence_level 为人体/临床类值，先读 `backend/app/services/evidence_level.py` 确认枚举取值再写死）；同分按产品 id 升序
- 响应：`{"canon": "美白", "total": 3670, "items": [{"id", "name", "brand", "score", "ingredient_hits": N, "human_evidence": M}]}`；只含 items 有断言命中的产品；排除法规/防腐族（口径同 fingerprint.py）
- 顶部口径由前端展示：「按成分证据强度排序，非效果排名」
- TDD：自建 Product/Ingredient/EfficacyAssertion/Evidence 行（参照 test_pagination.py fixture 模式 + test_fingerprint.py 的断言建行方式），测：排名分计算、真人级加权、未知 canon 422、limit 生效、无命中产品不进榜
- 提交：`feat: 功效产品榜接口（按成分证据族排名）` 并 push

Task 4/6 的 EfficacyBoard 数据源相应改调本接口（行内一句话理由用 `ingredient_hits`/`human_evidence`：「N 个成分有证据 · M 条真人实验」）；产品库工具条的功效胶囊仍走 Task 1 的 claims 文本筛选（语义不同：胶囊=「官方宣称筛选」，榜单=「成分证据排名」，两处文案各自注明口径）。防晒族无证据数据，Tab 从四枚改为三枚（美白/抗老/保湿）+ 祛痘，与证据族一致。
